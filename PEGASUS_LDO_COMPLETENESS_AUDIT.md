# PEGASUS LDO/EFG Mathematical-Completeness Audit vs MSD-III

Generated 2026-07-06 by a 70-agent adversarially-verified review reading PEGASUS_MSD_III.md + the LDO/EFG implementation. Each requirement is a NORMATIVE MSD-III clause; status is what the code actually does on the live national investigate path (run_ldo). This is the LDO build ledger — update the Status column as packages land.

## Progress log

- **WP1 — §III.4(5) quadratic prior-regularizers — DONE (commit f774e28).** Temporal-smoothness quadratic (was a *blocker*, entirely missing) and the disease-`L_D` Laplacian quadratic `(γ/2)·tr(Sᵀ L_D S)` (was only an adaptive-ℓ1 discount) are now in the LVGLASSO S-step as a Lipschitz-stable proximal-gradient step; `DiseaseGraph`→`L_D` exposed via `disease_laplacian_matrix`; wired through `fit_lagged_links`→`run_ldo` (default γ=0.1, byte-identical at γ=0), stability refits inherit it. Proof-of-capability test added. **Still open in this clause:** the spatial BYM/ICAR *varying-coefficient field* (`spatial_field_ref`) — folded into **WP2** (it is the spatial-hierarchy carrier).
- **Next: WP4** (HSIC residual completeness), then **WP5** (causal ladder).

## Subsystem verdicts

| gaps/reqs | subsystem | verdict |
|---|---|---|
| 1/11 | CPW sparse+low-rank precision decomposition (Ω = S − L; Chan | Mathematically complete and wired: the S−L decomposition is the prescribed CPW estimator with a faithful ADMM objective and correct three-block prox steps, live on the national inv |
| 3/9 | Spatial GMRF whitening + covariance metric (§III.4 the L_W s | The prescribed GMRF operator κI+L_W is present, correct, built from the real SpatialWeightGraph, and wired matrix-free/sparse into the live national estimator via whitening; but th |
| 4/9 | Lag-extended links + temporal smoothness + stability selecti | Lag structure, directed distributed-lag readout, and S/L mutual exclusion are present, correct, and wired; but the §III.4(5) temporal-smoothness regularizer is entirely absent (blo |
| 3/5 | Disease-structure prior L_D on the LDO precision (§II.6 / §I | NOT mathematically complete: the prescribed Laplacian-quadratic disease smoothing (γ/2)·tr(Sᵀ L_D S) is MISSING; L_D is realized only as an adaptive-ℓ1 edge-selection weight, which |
| 5/8 | Residual nonlinear audit (§III.6 HSIC, repositioned) — live  | Partially complete: the residual, HSIC-approx, BH-FDR, detector-not-engine, and a spatial structured null ARE present and wired — but the §6.8 null is NOT panel-type-selected (hard |
| 5/8 | Causal escalation (Part IV — the typed causal ladder on top  | Partially complete: Rung-0 and the LiNGAM half of Rung-1 are genuinely present, correct, and wired on the live national path; but collider/v-structure detection is orphaned (implem |
| 8/14 | Certification, bounded exhaustiveness, and the compute envel | NOT mathematically complete vs MSD-III: the certification gate and the LDO fit-path/residual-scan envelope refusals are present+wired, but the entire Part VIII bounded-exhaustivene |
| 10/12 | Computation & Scale mandates as they constrain the inference | NOT mathematically complete vs Part V. The headline prescribed capability — the §V.5 Adaptive Precision Controller — is implemented but ORPHANED (zero callers on the run_ldo path;  |
| 6/17 | LDO orchestration + field assembly + output contract (Part I | The LDO backbone (margins->precision/CPW split->lags->residual HSIC->certify->orient) is genuinely wired end-to-end on the national investigate path and is mathematically faithful  |
| 4/6 | EFG — legality algebra + measured-quantity objects (MSD-III  | The legality/provenance algebra and the SHE/EFG state lattice are PRESENT, faithful, and WIRED on the live compile path; but the central §II.3 reconception — the EFG emitting measu |

## Confirmed gaps (adversarially verified)

Status legend: PENDING | IN-PROGRESS | DONE. Build order (user-set): WP1 (three quadratic prior-regularizers) -> WP4 (HSIC completeness) -> WP5 (causal ladder) -> then WP6/WP3/WP2/WP7/WP8.

### BLOCKER (10)

**BLO-1** [missing] — §III.4(5); §III.7 ('GMRF/hierarchy quadratics'); Concepts index §5.4 ('temporal smoothness shrink estimates')  — _Status: PENDING_
- REQ: Temporal smoothness regularizer: §III.4(5) prescribes temporal smoothness (alongside the spatial GMRF L_W and disease Laplacian L_D) as a prior-regularizer that shrinks estimates across neighboring time points. It is one of the five simultaneous structural assumptions.
- FIX: Add a temporal-smoothness Laplacian quadratic (gamma/2)*tr(S^T L_time S) to the LVGLASSO objective over the lag-extended feature set, tying adjacent-lag precision blocks (and, where a genuine time axis is carried, adjacent time points). Concretely: (1) build L_time as a chain/path graph Laplacian over the K+1 lag positions (nearest-lag differencing), lifted to the p*(K+1) feature space by Kronecker with I_p so it penalizes second differences of a source->target profile across consecutive lags {S^{(k,0)}_{ij}}; (2) fold it into fit_sparse_plus_lowrank as a differentiable ridge-like term absorbed into the R-step via an augmented prox (or an extra ADMM splitting variable), guaranteeing a strict no-op at gamma=0 so the CPW S/L split and all existing tests are unperturbed — mirroring the exact prescription written for the un-built DIS-04b disease quadratic in disease_prior.py:12-16; (3) thread a gamma parameter through fit_lagged_links (lags.py:70) and run_ldo (orchestrator.py:107). This makes the discovered distributed-lag response_curve smooth-regularized rather than a noisy free read-off, satisfying the fifth §III.4 structural assumption. Reuse the same prox scaffolding to finally realize L_D's quadratic form, retiring the shared DIS-04b debt.

**BLO-2** [present_divergent] — MSD-I §6.8 Null & FDR Registry (per-support null column); §6.9 (monthly: season_preserving_moving_block_circular_shift preserving seasonality, lag-1 autocorrelation, spatial-block dependence); referenced by MSD-III §III.6 and amendment list  — _Status: PENDING_
- REQ: HSIC null must be the §6.8 STRUCTURED null keyed to the panel support (annual municipal → spatial-block + cyclic time shift; monthly seasonal → season-preserving moving-block circular shift; facility stock → intra-UF spatial swap; etc.), NOT an iid permutation. The null must preserve the panel's temporal/seasonal + spatial-block dependence so HSIC tests dependence BEYOND shared spatio-temporal clustering.
- FIX: Wire the existing panel-aware registry into the live residual scan and select by panel support instead of by spatial-block count. Concretely: (1) Have run_ldo/investigate derive and pass a panel_type into scan_residual_nonlinear_edges: infer it from field geometry/metadata — T divisible by 12 with T>=12 and monthly cadence -> 'monthly_seasonal_panel'; annual municipal cadence -> 'annual_municipal_panel'; single-period facility stock -> 'facility_stock'; census cross-section -> 'cross_sectional_census'; sparse/stratified -> 'sparse_stratified'. (2) Inside scan_residual_nonlinear_edges, replace the hardcoded lines 210-222 with regime = select_null_regime(panel_type); null_strategy = regime.null_strategy; and build the support dict the chosen strategy needs — for monthly/annual pass support={'panel_shape':[space,time], 'spatial_blocks': uf_labels_per_space} so generate_null_indices dispatches to season_preserving_moving_block_circular_shift (monthly) or spatial_block_cyclic_time_shift (annual); for facility_stock pass {'uf_strata': uf_labels}; census {'adjacency_components': ...}; sparse {'strata': ...}. Note the residual scan currently permutes over complete-case CELLS (a flattened, possibly non-rectangular subset), whereas the season/annual generators require a rectangular panel_shape with space*time==n; so also (3) build the permutation index set on the FULL space*time grid and restrict/reindex to the observed cells, or gate the structured null behind a full-rectangle check and only then apply it. (4) Call assert_monthly_null_preserves_season(regime) as a guard before running so a monthly panel can never silently fall back to a season-breaking null. (5) Preserve the existing §6.8/§6.9 descriptive-only downgrade when spatial_blocks<5 or temporal_blocks<5 (descriptive_only_when_insufficient_blocks already exists in nulls.py). This makes the live null match the per-support registry and stops the national monthly panel from using the facility-stock intra-UF swap.

**BLO-3** [present_divergent] — §III.6 (the audit is a required LDO layer, run 'after fitting'); §V.1 refuse-not-subsample; §II.7 multi-resolution as the fit-doesn't-fit remedy  — _Status: PENDING_
- REQ: The prescribed residual nonlinear audit MUST actually run on the live national investigate path (a run that cannot fit must refuse per §V.1, but §II.7 multi-resolution/tiling is then the prescribed continuation so the capability is still delivered — not silently skipped).
- FIX: Deliver the §II.7 remedy on the residual path (not just the linear backbone) so the §III.6 capability is delivered at a coarser resolution instead of being swallowed:

1. Give scan_residual_nonlinear_edges a tiled/multi-resolution mode instead of only raising. When estimate_residual_scan_bytes exceeds the budget, either (a) coarsen the lattice (aggregate S·T to region×quarter or muni×year until the per-variable representation cache fits the envelope) and run the scan there, tagging emitted nonlinear_residual records with the resolution actually used and widened uncertainty per §V.6; or (b) block the p²/2 pair scan over envelope-sized variable/cell tiles, streaming per-variable representations so peak memory is O(tile) not O(p·n_eff) — mirroring the population-tensor locality-blocking contract in §V.1(b). Refuse only if even the coarsest envelope-sized form is infeasible.

2. In orchestrator.py:159-167, on ScaleExceedsEnvelopeError specifically, invoke the coarsened/tiled residual scan rather than only catching into diagnostics['residual_scan_error']. Reserve the diagnostic-swallow for genuinely-unrecoverable failures, and stop using a bare `except Exception` that masks all errors as 'additive'.

3. Wire run_multiresolution_ldo (or an equivalent) into run_investigate so national monthly runs use the coarse→fine strategy end-to-end, and ensure its fine pass carries the tiled residual scan from (1) rather than re-hitting the flat refusal.

4. Fix the null so §III.6's named 'seasonal' structure is testable: use a restricted permutation that also respects the seasonal/temporal block (e.g. within-UF AND within-month-of-year swaps), not spatial-block-only.

5. Reconsider the complete-case gate (residual_scan.py:188): use per-pair pairwise-complete cells or weighted residuals so sparse variables do not collapse the shared sample to an unrepresentative complete-case subset, consistent with §III.2.

**BLO-4** [missing] — §V.6(3) (l.301), §IX.3 (l.404), §IX.4 (normative battery)  — _Status: PENDING_
- REQ: §V.6(3)/§IX.3 Exact certifies approximate: a state-scale EXACT run MUST agree (within propagated bounds) with the national approximate run where they overlap; disagreement beyond bounds rejects the approximation loudly. Marked NORMATIVE.
- FIX: Build an exact-certifies-approximate runner (place in src/pegasus/validation/ and wire it as an optional certification pass reachable from the investigate workflow, gated to overlapping variables). Concretely: (1) Add an exact-solve mode to the LDO. In lowrank.fit_sparse_plus_lowrank pass randomized_factors=False AND ensure the ADMM runs to a tight tolerance/converged; add an assemble/run_ldo path parameter (e.g. exact=True) that forces exact readout everywhere approximation dials exist (randomized SVD off, full Hutchinson/exact logdet, no rank truncation below numerical rank). (2) Run the LDO on a state-scale subset (a single UF, or a bounded S·T that fits the envelope) in exact mode, and the national run in approximate mode, restricting both to the overlapping variable/edge set (reuse resolution.restrict_variables for the variable intersection). (3) For each overlapping edge, compare the approximate partial-correlation / factor-loading estimate against the exact one using the propagated uncertainty already carried on LinkRecord.uncertainty (widen by both runs' bounds). Define agreement as |approx − exact| <= k·sqrt(var_approx + var_exact) for a stated k. (4) On any edge whose disagreement exceeds the propagated bound, raise loudly — a new error (e.g. ApproximationCertificationError in certify.py) or a hard 'certification_rejected' status — never a silent merge; record the offending edges and the bound. (5) Surface the pass/fail in diagnostics and make it a normative gate per §IX.4 (a national approximate run is not certified until its overlap with a state-exact run passes). Add one focused proof-of-capability test: plant an approximation error large enough to exceed bounds and assert the runner rejects loudly, and a within-bounds case that certifies.

**BLO-5** [orphaned] — §VIII.2(1) (l.380)  — _Status: PENDING_
- REQ: §VIII.2(1) Screen on sensitivity, not the aggregate mean: coarse passes must screen on subgroup heterogeneity / dispersion / max-subgroup signal (a sensitive recall-tuned filter), converting the coarse pass from a test into a filter.
- FIX: Wire the sensitivity screen into the live coarse->fine path and stop drilling on the certified 'selected' TEST. Concretely: (1) In resolution.py, replace _candidate_pairs' certification_status=='selected' gate with should_drill_down applied to per-subgroup effect vectors from the coarse fit. This requires the coarse LDO to expose subgroup-resolved effects (e.g. per-UF or per-region edge coefficients / per-subgroup lagged-link strengths) rather than only the pooled certified edge — compute subgroup_effects for each candidate (source,target) pair and drill when should_drill_down fires on heterogeneity OR max-subgroup, NOT on the pooled selected flag. (2) Have run_multiresolution_ldo populate and return a CoverageManifest: mark_searched for drilled pairs at fine resolution, mark_unsearched (with reason='below_sensitivity_thresholds' and the resolution/conditioning-set) for pruned pairs, and set the sparsity_of_truth_assumption string — never a silent gap. (3) Add §VIII.2(2) random deep audits: on a random sample of pruned pairs run the fine pass anyway and record random_audit_false_negative_rate on the manifest. (4) Make run_investigate call run_multiresolution_ldo (coarse year x muni -> fine month x muni) on the national path, or thread the sensitivity screen + CoverageManifest into run_ldo directly, so the normative filter is actually reached on real runs and the manifest is emitted alongside Hypotheses.parquet. Export exhaustiveness symbols from ldo/__init__.py so they are part of the package API.

**BLO-6** [missing] — §VIII.2(2) (l.381)  — _Status: PENDING_
- REQ: §VIII.2(2) Random deep audits: on a random sample of pruned branches, run the full fine analysis to MEASURE the false-negative rate empirically.
- FIX: Implement the random-deep-audit sampler in run_multiresolution_ldo (src/pegasus/ldo/resolution.py) and populate random_audit_false_negative_rate on a CoverageManifest actually returned on the national path. (1) After computing candidates and the pruned variable set (fine_field variables NOT in candidate_vars — the branches the coarse sensitivity filter pruned), draw a seeded random sample of pruned branches of size min(m, audit_budget) via np.random.default_rng(seed) for reproducibility. (2) For each sampled pruned branch, run the FULL fine analysis anyway: build the restricted GaussianField including that pruned pair's variables and call run_ldo at fine_K, exactly as the candidate path does. (3) Count a false negative whenever a sampled pruned branch yields a fine-pass certification_status=='selected' edge (a link the coarse filter missed). Estimate FN_rate = (#audited branches firing selected)/(#audited), with a Wilson CI for the small-sample rate. (4) Construct a CoverageManifest (from pegasus.ldo.exhaustiveness), record searched/unsearched regions via mark_searched/mark_unsearched for candidate vs pruned branches, set manifest.random_audit_false_negative_rate = FN_rate, and return it inside MultiResolutionRun (add a coverage_manifest field). (5) Surface FN_rate into LDO diagnostics/output (src/pegasus/ldo/output.py, certify.py) so the §IX.4 certified sentence can state the measured miss rate. Add one focused acceptance test: plant a fine-only cancellation/Simpson edge with no coarse footprint, confirm the coarse filter prunes it, and confirm the random audit's measured FN_rate is > 0 and recovers the planted branch when sampled.

**BLO-7** [orphaned] — §VIII.2(3) (l.382-384)  — _Status: PENDING_
- REQ: §VIII.2(3) Typed coverage manifest: everything not searched recorded as a typed `unsearched` region (resolutions/conditioning-sets/functional-forms covered, where pruned and why); sparsity-of-truth assumption stated, not hidden. Never a silent gap.
- FIX: Instantiate and emit a CoverageManifest on the live investigate path. (1) In run_ldo (orchestrator.py), build a CoverageManifest during the fit: call manifest.mark_searched for the resolution actually fit (from gf.resolution) and the conditioning/functional coverage actually exercised (linear joint precision at lag order K; HSIC residual pass when run_residual_scan and residual_scan_error is None); call manifest.mark_unsearched for what was pruned or skipped, each with a typed reason — e.g. requested_K vs the adaptive K cap (reason=adaptive_lag_cap p/T), finer resolutions not run (reason=single_resolution_fit; only gf.resolution examined), the residual/nonlinear functional form when run_residual_scan is False or _residual_error is set (reason=residual_scan_skipped/errored), and low-rank-unconverged S/L splits (reason=lowrank_unconverged, reusing the §V.6 gate). Keep the default sparsity_of_truth_assumption string. Attach it as LDORun.coverage (new field) and mirror a serializable form into diagnostics. (2) In run_investigate, thread ldo_run.coverage into InvestigateResult (new coverage field) and persist it — either write Coverage.parquet/coverage.json alongside Hypotheses.parquet in write_hypotheses (add a companion writer), or add a coverage key to the run bundle — so the typed unsearched regions and the stated sparsity-of-truth assumption reach the user. (3) When run_multiresolution_ldo is used, have it mark the coarse resolution + candidate-variable set as searched and record every non-candidate pair NOT re-examined at fine resolution as unsearched (reason=pruned_not_a_coarse_candidate), merging into the same manifest. Add one proof-of-capability assertion (extend tests/synthetic/test_exhaustiveness.py or a new integration test) that a real run_investigate result exposes a non-empty coverage manifest with at least one typed unsearched region and a non-empty sparsity_of_truth_assumption.

**BLO-8** [orphaned] — §V.5 (l.289-293)  — _Status: PENDING_
- REQ: Adaptive Precision Controller: an anytime computation with a current best answer + current uncertainties, a scheduler that sets every approximation dial (SVD rank, Hutchinson probes, sketch size, CG iters, resolution depth, bootstrap count) per-subproblem by value-of-computation, and the self-correction property (when NUMERICAL error dominates a decision-relevant edge, escalate THAT edge to exact; leave data-limited edges alone), spending until a user/policy target uncertainty is met or budget exhausts, then reporting met vs approximation-limited (typed, never silent).
- FIX: Wire the §V.5 controller into the live investigate path so uncertainty actually drives compute. Concretely: (1) In run_ldo (orchestrator.py), replace the fixed scalar knobs with an anytime loop governed by a Budget: run an initial cheap-precision pass, then for each decision-relevant edge estimate its Quantity by separating statistical uncertainty (from stability-selection recurrence / subsample variance across n_subsamples — irreducible by compute) from numerical uncertainty (the reducible approximation error of each dial: low-rank ADMM randNLA sketch size / rank, Hutchinson/logdet probe count, CG/MINRES iterations, HSIC residual-scan bootstrap count, and the lag/resolution depth). Feed those Quantities to adaptive_precision_run(target=policy_target, budget=Budget(total=...)) and, for each edge the scheduler selects for escalation, actually RE-SOLVE that subproblem at exact/higher precision (more CG iters, larger sketch, exact logdet, deeper resolution via resolution.py) — i.e. make exact_cost/escalation a real recompute callback rather than the current toy 'numerical_uncertainty=0.0' stub. (2) Change the escalation step in adaptive_precision_run to invoke a per-subproblem refine(quantity)->new_numerical_uncertainty hook instead of hard-setting 0.0, so escalation reflects the true post-recompute error. (3) Surface target uncertainty and budget as parameters of investigate.py (l.140+) and pass a real Budget/target into run_ldo, replacing the fixed K/lambda1/lambda2 fiat. (4) Emit the ControllerReport (met / approximation_limited / data_limited) into ldo_run.diagnostics and onto each LinkRecord as a typed status, so the certifier/output distinguishes met vs approximation-limited vs data-limited (never silent) — satisfying the §V.6 typed-reporting tie-in. (5) In run_multiresolution_ldo, drive the coarse->fine decision (which candidate edges get a finer resolution pass) from the same VoC scheduler rather than the fixed fine_K + carry-all heuristic, so 'resolution depth' becomes a controller-set dial. This makes uncertainty an actionable control signal on the national path and gives numerically-dominated decision-relevant edges genuine auto-escalation to exact while leaving data-limited edges alone.

**BLO-9** [orphaned] — §II.3 (lines 125-132)  — _Status: PENDING_
- REQ: The EFG's terminal output is a MeasuredQuantity object {numerator_count, exposure/denominator, offset_semantics, structure(axes,strata), provenance, uncertainty(state tensor)} — NOT a materialized rate. The LDO consumes count+exposure so EFG and LDO 'speak one language: count + exposure + structure'.
- FIX: Wire the exposure through the compile→panel→LDO path so run_ldo consumes count+exposure instead of the divided rate:

1. Panel carries exposure, not just the rate. In she/panel.py, for RN/ratio fields, load the {field.id}.measured_quantity.parquet sidecar (numerator_count, exposure) rather than (or in addition to) the rate 'value' column. Materialize a per-variable exposure aligned to the same (p, S, T) cell index the value tensor uses (_align_field / value_frame). Either add a parallel exposure_frame keyed identically, or attach exposure as a typed companion on CommonPanel so assemble_ldo_tensor can surface it. Keep non-ratio/intensive fields (no exposure) as-is — they must remain None so gaussianize_field falls back to the plain margin (margins.py:110-113 already handles per-variable None).

2. assemble_ldo_tensor builds the (p, S, T) exposure array. In ldo (assemble_ldo_tensor / LDOField), construct an exposure ndarray of shape (p, S, T) matching the value tensor, filled from the panel's per-variable exposure and NaN for variables with no denominator, so count_exposure_gaussianize's mask (exposure>0) selects exactly the extensive count variables.

3. Pass exposure from investigate.py. At workflows/investigate.py:179, thread the assembled exposure array into run_ldo(panel, ..., exposure=exposure) (or, cleaner, have _prepare_ldo_inputs pull exposure off the CommonPanel when source is a CommonPanel, so no signature change at the call site). Then gaussianize_field(exposure=...) routes extensive variables through count_exposure_gaussianize (the Poisson-offset margin) and intensive ones through the ordinary margin.

4. Regression guard. Add one focused test that a compiled RN field with a real exposure produces, on the live investigate path, a gaussianized latent for that variable that differs from the rate-only path (e.g. two cells with equal rate but different exposure map to the same latent value, and count-variance is preserved) — pinning that the offset margin is actually reached, not defaulted to None.

This eliminates the §II.3 waste (LDO reverse-engineering structure from rates) and makes EFG and LDO speak count+exposure on the national inference path, using machinery (margins.count_exposure_gaussianize, the MQ sidecar) that is already built — the fix is wiring, not new math.

**BLO-10** [orphaned] — §II.3 (line 132), §I.2, §III.5  — _Status: PENDING_
- REQ: Normalization belongs in the model as an offset: log E[count] = log(exposure) + effects, with the rate as the implied view exp(effects) carrying correct count-variance — the LDO must model count + exposure directly (the count-with-exposure margin for extensive quantities).
- FIX: Thread a per-variable (p,S,T) exposure tensor from the compiled panel into run_ldo so the count-with-exposure margin becomes live for extensive count variables. Concretely: (1) At compile time, have the panel/assembler ingest the already-emitted MeasuredQuantity sidecars ({field.id}.measured_quantity.parquet, offset_semantics="log_exposure") rather than only the derived rate parquet — carry, for each extensive field, its numerator_count as X[j] and its exposure as a parallel exposure[j] scattered onto the same (space,time) grid via the identical s_index/t_index scatter used for X in assemble_ldo_tensor (a variable's exposure NaN/≤0 where the denominator is absent). Non-extensive/intensive fields (context ratios, compositional shares) carry exposure=NaN so gaussianize_field correctly routes them to the rank-PIT margin. (2) Extend LDOField/assemble_ldo_tensor to return an optional aligned exposure ndarray (variable-axis order matching field.variables, respecting keep_variables), and have _prepare_ldo_inputs pass field.exposure into gaussianize_field. (3) In run_investigate, build exposure from the panel's MeasuredQuantity refs keyed by field_id and pass exposure= into run_ldo (remove the "deferred" note). (4) Add a national-path assertion/diagnostic (e.g. n_count_exposure_margins > 0 in run_ldo.diagnostics) and an integration test that a compiled run with count fields actually routes ≥1 variable through count_exposure_gaussianize, so the wiring cannot silently regress to all-rank-PIT again. This makes the EFG's count+exposure MeasuredQuantity the LDO's modeling input (one language, §II.3) and preserves count-variance via the log(exposure) offset instead of Gaussianizing a pre-divided rate.

### MAJOR (25)

**MAJ-1** [present_divergent] — §III.4(5) ('shrink estimates across neighbors'; 'Interaction … varying-coefficient structure'); §III.3; §III.7 LinkRecord.spatial_field_ref  — _Status: PENDING_
- REQ: §III.4(5): the spatial GMRF L_W is a prior-regularizer that shrinks estimates across neighbors — i.e. a BYM/ICAR spatial smoothness prior on a spatially-varying field/coefficient (varying-coefficient structure; §III.3 multiresolution readout; §III.7 spatial_field_ref per link).
- FIX: Add a genuine spatial prior-regularizer distinct from the whitening metric, and populate spatial_field_ref. Concretely: (1) BYM/ICAR field per varying-coefficient — for links/coefficients declared spatially-varying (§III.7 spatial_field_ref, §III.2 effect-modification), estimate a per-locality effect β_s and add the GMRF quadratic (τ/2)·βᵀ L_W β to the penalized objective in fit_sparse_plus_lowrank / a companion varying-coefficient solver, so neighboring localities' coefficients shrink toward each other (this is the ADDITIVE prior term the current objective lacks — separate from the correlation-whitening step, which stays as the space-nuisance remover). (2) Multiresolution readout (§III.3): decompose each spatial coefficient as national+region+state+muni components with a coarse→fine shrinkage prior (the SPDE/INLA-style hierarchy), reading fine detail only where data supports it. (3) Effect-modification interaction (§III.2/§III.4(5)): represent 'is X→Y stronger where sanitation poor' as an explicit interaction/varying-coefficient term whose spatial surface is the estimated β_s field. (4) Write spatial_field_ref on each LinkRecord that carries a fitted spatial-heterogeneity field (currently a dead field), and surface it in to_link_records. This must be a new estimation component (Task #29 MR-01 GMRF), gated by the §V compute envelope, not a rewrite of the existing whitening path.

**MAJ-2** [present_divergent] — §III.4(5)  — _Status: PENDING_
- REQ: Prior-regularization companion: disease Laplacian L_D as a smoothness quadratic +(gamma/2)tr(S^T L_D S) smoothing connection profiles across the hierarchy (§III.4(5), same clause as temporal smoothness).
- FIX: Implement DIS-04b: add the Laplacian-quadratic smoothness term +(gamma/2)tr(S^T L_D S) to the LVGLASSO ADMM objective as a smoothness prox in the S-step of fit_sparse_plus_lowrank (lowrank.py). Concretely: (1) accept a p*p (tiled to (p(K+1))^2) disease Laplacian L_D matrix alongside the existing per-pair penalty_matrix; (2) in the S-step, replace the pure soft-threshold with the prox of lambda1*||S||_{1,off} + (gamma/2)tr(S^T L_D S), i.e. solve (rho I + gamma L_D_op) applied to S then soft-threshold, so related diseases' connection profiles (rows of S) are shrunk toward each other, not merely thresholded more leniently; (3) require gamma=0 to exactly recover the current CPW S/L split (a hard test) so the incoherence identifiability at readout is unperturbed; (4) build L_D from DiseaseGraph structural adjacency (degree - affinity, structural legality_class only, reusing the circularity guard in variable_affinity) and tile it lag-invariantly exactly as tile_penalty_across_lags does; (5) wire a gamma default through run_ldo/investigate.py parallel to the existing disease_penalty path and record it in diagnostics. This makes L_D a true profile-smoothing quadratic, the direct analogue of L_W's realized (kappaI+L_W)^{1/2} whitening, matching the §III.4(5) grouping. Retain the adaptive-l1 penalty as a complementary mechanism (edge selection) rather than replacing it.

**MAJ-3** [present_divergent] — §III.7; §XI ('edges must survive subsampling'); §5.1 LDO-05  — _Status: PENDING_
- REQ: Stability selection: refit on lattice subsamples and keep only edges recurring above a frequency threshold; edges below threshold marked descriptive (not promoted). Multiplicity controlled without per-pair tests.
- FIX: Bring latent_shared under stability selection to match §III.7/§XI, without disturbing the (correct) noise-floor gate:

1. In edges.py `_subsample_edges`, additionally record shared-driver keys from each refit: after the existing loops, iterate `res.latent_shared` and add a canonical key `_edge_key(min(src,tgt), max(src,tgt), 0)` (or a distinct namespace, e.g. lag sentinel like -1, to avoid colliding with contemporaneous keys for the same pair) to `seen`. Because latent_shared is an unordered pair, canonicalize source/target ordering before keying so subsample-to-subsample matching is order-invariant.

2. In edges.py `to_link_records`, for the latent_shared loop (183-194) look up the canonical key in the stability dict and attach it: `stab = stability.get(canonical_key)`, and set the provisional status the same fail-open-then-certify way as the other edge types (or directly gate here).

3. In certify.py `certify_link`, change the latent_shared branch (47-49) to require recurrence: `stab = record.stability; if stab is None or stab < policy.min_stability: return descriptive; else selected` -- keeping the low_power/unconverged early-returns above it intact. This makes a shared-driver that no subsample reproduced descriptive rather than selected.

4. Keep the lowrank.py 2%-noise-floor as-is; the two gates are complementary (noise floor removes PSD-tail artefacts within a single fit; subsampling controls edge-set multiplicity across refits).

This closes the only surviving divergence; do NOT touch the lagged/contemporaneous path, which already satisfies the spec via certify_links.

**MAJ-4** [missing] — §III.4(5) + §III.7 ('graphical-lasso (ℓ1) + nuclear-norm (low-rank) + GMRF/hierarchy quadratics'); §II.6 table row 'Condition … disease graph L_D'; §5.2  — _Status: PENDING_
- REQ: L_D must enter the precision estimate as a prior-regularizer that 'shrinks estimates across neighbours/parents' in the CID-10 hierarchy — i.e. the disease Laplacian is one of three GMRF/hierarchy QUADRATICS added to the penalized pseudo-likelihood, the direct disease-axis analogue of the spatial GMRF L_W. In the standard form this is a term (γ/2)·tr(Sᵀ L_D S) that smooths each variable's dependency PROFILE toward those of its hierarchical neighbours (structurally-close diseases should have similar rows of the precision).
- FIX: Implement the §III.4(5) Laplacian-quadratic in the ADMM S-step of fit_sparse_plus_lowrank (lowrank.py), so the disease axis gains the same quadratic coupling the spatial axis already has. Concretely: (1) Plumb the tiled feature-space disease Laplacian L_D_feat (p·(K+1) square, built by tiling graph.laplacian() over lag blocks exactly as tile_penalty_across_lags does for the ℓ1 weights, then subset to kept features via np.ix_(kept, kept)) into fit_sparse_plus_lowrank as a new argument disease_laplacian, passed from lags.fit_lagged_links / orchestrator alongside the existing disease_penalty. (2) Add the quadratic (γ/2)·tr(Sᵀ L_D S) to the objective and realize it as a prox/linear term in the S-step. Since tr(Sᵀ L_D S) is smooth with gradient γ·L_D S (symmetrized: γ·(L_D S + S L_D)/2), fold it into the S-update either (a) by adding a ridge-like linearized term so the S-step solves argmin_S (rho/2)||S − (R+L+U)||² + λ1‖S‖₁,off + (γ/2)tr(Sᵀ L_D S), i.e. soft-threshold on the shifted target (rho·(R+L+U))/(rho·I + γ·L_D) — practically a per-iteration linear solve (κI+L_D preconditioned CG, matrix-free per §V.2) followed by the off-diagonal soft-threshold; or (b) introduce a separate ADMM splitting variable for the smoothness term. (3) CRITICAL invariant (the docstring already flags it): at γ=0 the update MUST reduce exactly to the current _soft_threshold_offdiag path so the CPW S/L incoherence split and all existing recovery tests are byte-preserved. (4) Expose γ (disease smoothness strength) as a run parameter defaulting to 0 (honest no-op) and set >0 only when a structural DiseaseGraph is supplied; record disease_quadratic_applied in the run diagnostics next to disease_prior_applied (orchestrator.py:205). (5) Add one focused proof-of-capability test (per the project's "one focused proof per feature" directive): plant two hierarchically-adjacent disease variables whose true precision rows are near-identical, verify the γ>0 fit shrinks their estimated dependency profiles toward each other (smaller row-difference norm) versus γ=0, while γ=0 leaves the CPW split unchanged. This retires the DIS-04b scope note in disease_prior.py:10-16.

**MAJ-5** [missing] — §III.3 (multiresolution hierarchical shrinkage, 'chapter + block + category + leaf deviations'); the L_D prior is the disease-axis carrier of this (§III.4(5))  — _Status: PENDING_
- REQ: Multiresolution hierarchical shrinkage on the disease axis — sum-of-scales (chapter + block + category + leaf deviations) with each fine level shrunk toward its coarse parent (borrowing strength / small-area BYM analogue), the mechanism that 'finishes off sparsity' for rare disease leaves.
- FIX: Add a genuine multiresolution sum-of-scales prior on the disease axis (the §III.3 'borrow strength across scales' device), realizing L_D as a hierarchical GMRF rather than a scalar l1 discount:

1. Build a disease-axis scale-incidence structure from the CID-10 tree: for each leaf variable define its ancestry (leaf -> category -> block -> chapter). This already exists implicitly in DiseaseGraph.codes; expose an aggregation/design operator Z_level mapping fine cells to each coarse level.

2. Decompose the disease-axis latent effect additively across scales: theta_leaf = mu_chapter + delta_block + delta_category + delta_leaf, each level a random-effect component with its own precision tau_level (a BYM-style multilevel model). Estimate the coarse components from pooled data and shrink each fine deviation toward zero with tau proportional to inverse level variance — so a rare leaf with little data collapses onto its category/block/chapter parent estimate (the 'finish off sparsity' behavior), while a data-rich leaf retains its own deviation.

3. Wire this as a quadratic GMRF penalty on the precision estimator, not a per-pair l1 weight: assemble L_D as the hierarchical graph Laplacian coupling each fine node to its parent (Q = sum_level tau_level * Z_level^T Z_level), and add the smoothness term (gamma/2)*tr(S^T L_D S) — first delivering the intermediate DIS-04b Laplacian-quadratic form the docstring already scopes (a prox in the ADMM S-step that must be a no-op at gamma=0 to preserve the CPW S/L split), then extending it to the full per-scale variance-component structure with tau's fit/tuned per level.

4. Keep the existing structural-only circularity guard (context_derived graphs refused) and lag-invariance tiling; expand tile_penalty_across_lags analogue so the quadratic couples within- and cross-lag disease blocks consistently.

5. Cover with one proof-of-capability test: plant a rare leaf with near-zero direct signal under a well-estimated parent and assert its posterior effect is pulled toward the parent (borrowing strength) rather than merely surviving a lower selection threshold. This is the deferred MR-01 disease-axis work (task #29).

**MAJ-6** [orphaned] — §II.6 ('disease graph L_D'); §5.2 ('graph.py DiseaseGraph → L_D')  — _Status: PENDING_
- REQ: DiseaseGraph.laplacian() as the exposed disease smoothness operator L_D = D − W for the LDO prior.
- FIX: Implement DIS-04b: add the Laplacian smoothness quadratic +(γ/2)·tr(Sᵀ L_D S) to the LVGLASSO S-step so L_D's numeric value actually regularizes the precision. Concretely: (1) in orchestrator.run_ldo, obtain L_D = disease_graph.as_prior() (guarded Laplacian over graph.codes), align/reindex it to gf.variables (rows/cols for variables absent from the graph → zero), and tile it across the K+1 lag blocks the same way tile_penalty_across_lags does for the ℓ1 penalty (relatedness is lag-invariant). (2) Thread a gamma coefficient and the tiled L_D through fit_lagged_links → fit_sparse_plus_lowrank. (3) In the ADMM S-step, add a smoothness prox/gradient for (γ/2)·tr(Sᵀ L_D S) (a linear term γ·L_D·S in the S-update), keeping the CPW S/L split invariant at γ=0 so it does not perturb the incoherence-gated readout (Appendix F.1) or the existing ℓ1-affinity behavior. (4) Add one focused proof-of-capability test: two structurally-related disease variables with weak/noisy direct data should have their connection profiles shrunk toward each other under γ>0 (the direct analogue of L_W spatial whitening in lags.fit_lagged_links), and the fit at γ=0 must byte-match the current output. This makes the exact §III.4(5) object (L_D) enter the estimator instead of being computed-but-unused.

**MAJ-7** [present_divergent] — MSD-I §6.8 (FDR column); §III.6/§6.9 via reference; residual_scan docstring cites '§6.9 FDR'  — _Status: PENDING_
- REQ: FDR: apply the §6.8-registry false-discovery method to the residual-HSIC p-values before promotion (§6.8 assigns BY to annual/monthly cross-fitted panels, BH to cross-sectional, Storey-q to facility stock).
- FIX: Resolve the §6.8 support/FDR regime and thread it into the residual scan instead of hardcoding BH. Concretely: (1) Add a panel-support classifier that maps the CommonPanel/GaussianField temporal structure to a §6.8 support row (annual municipal panel and monthly seasonal panel → 'BY'; cross-sectional census → 'BH'; facility stock → 'STOREY_Q'; sparse stratified → 'BY') — the T dimension and cadence are already available on the field (field.shape T, and the cyclic/season metadata used to pick null_strategy). (2) Add an fdr_method parameter to scan_residual_nonlinear_edges (default resolved from the classifier, not a literal), pass it from run_ldo (orchestrator.py:161) after determining the panel support for the run. (3) Replace residual_scan.py:271 correct_p_values(pvals, method='BH') with method=<resolved> and set the LinkRecord fdr_method tag (residual_scan.py:283) from the resolved method (e.g. 'benjamini_yekutieli' for panels) so the recorded provenance matches the applied correction. fdr.py already supports BY/Storey with no change needed. For a national annual/monthly run this makes the residual-HSIC q-values BY-corrected (harmonic-inflated), restoring the intended conservatism for spatially dependent residual tests. Add one proof test: an annual-panel field resolves to BY and yields q-values inflated by the harmonic factor H(m) versus the current BH output.

**MAJ-8** [present_divergent] — MSD-I §6.8 (block-insufficiency rule: spatial_blocks<5 OR temporal_blocks<5 → descriptive); §III.8 certification gate  — _Status: PENDING_
- REQ: Descriptive-only gate: 'If fewer than five spatial OR temporal blocks exist, HSIC_residual → descriptive_association_only' — an edge without a valid structured null is surfaced but NOT certified as a discovery.
- FIX: In src/pegasus/ldo/residual_scan.py, compute the temporal-block count and fold it into the certification gate to match the §6.8 disjunction. Concretely: (1) derive temporal blocks from the panel time axis — the residual sample is drawn from complete-case cells whose time index is temporal_of_cell = (observed_idx % T).tolist() (mirror of the existing space_of_cell = observed_idx // T at :204); set n_temporal_blocks = len(set(temporal_of_cell)) (the number of distinct time points actually represented in the retained residual sample). (2) Replace :225 with the disjunctive predicate already available in nulls.py: from pegasus.ldo.nulls import descriptive_only_when_insufficient_blocks; sufficient_blocks = not descriptive_only_when_insufficient_blocks(spatial_blocks=n_spatial_blocks, temporal_blocks=n_temporal_blocks). This makes :284 certify 'selected' only when BOTH spatial>=5 and temporal>=5, and 'descriptive' otherwise — surfacing the edge without certifying it when the temporal span cannot license the null, exactly as the panel-aware hsic.py:473-477 path already does. Add a one-shot proof test (extend tests/synthetic/test_ldo_residual_null_structure.py) with >5 UFs but T<5 asserting the emitted LinkRecords carry certification_status='descriptive'.

**MAJ-9** [stubbed] — §IV line 254  — _Status: PENDING_
- REQ: Rung 1 — collider / v-structure detection (PC/GES/FCI): for a triple A–C–B with A,B non-adjacent, detect A→C←B when A⊥B marginally but A⊥̸B|C.
- FIX: In src\pegasus\ldo\orchestrator.py, after the existing orient_links call (line 191), add a collider-orientation pass that operates on the emitted contemporaneous/undirected edges as a graph. Concretely: (1) Build an adjacency set from the certified/selected LinkRecords keyed by (source_var, target_var) for undirected/contemporaneous edges. (2) Enumerate every unshielded triple A–C–B: C with two neighbors A,B such that A and B are NOT adjacent (no edge A–B in the record set). This is the standard v-structure precondition and requires only the edge set already in `records`, no new global search. (3) For each such triple, call the existing is_collider(data[A], data[B], data[C]) using the same orient_data map already built at orchestrator.py:188-190. (4) When is_collider returns True, orient the two edges A→C and C←B (replace source/target so C is the target of both) and append a warning tag e.g. "oriented_collider_vstructure" mirroring the "oriented_non_gaussian_lingam" tag, and add a diagnostics counter n_oriented_collider alongside n_oriented_lingam (orchestrator.py:208-209). (5) Add one focused proof-of-capability test (per the project's "one focused test per feature" directive) that plants A→C←B on non-adjacent A,B and asserts run_ldo emits both edges oriented into C — extend tests\synthetic\test_ldo_causal_orientation.py, which already tests the LiNGAM half through run_ldo. The full PC/GES/FCI global constraint/score-based structure search named in the spec is a larger build; the minimal spec-faithful delivery of the collider clause is the unshielded-triple enumeration above wired to the already-correct is_collider primitive, which closes the dead-primitive gap. If full PC/GES/FCI is required, add a constraint-based skeleton+orientation module (e.g. a PC-style CI-test skeleton over the LDO precision support) as a separate CAUSAL milestone, but the triple-enumeration wiring is the necessary first step and makes the existing is_collider live.

**MAJ-10** [orphaned] — §IV line 255  — _Status: PENDING_
- REQ: Rung 2 — quasi-experimental leverage (ITS around dated shocks, DiD, negative-control outcomes), triggered by detected structure.
- FIX: Wire Rung-2 into the escalation path in three steps. (1) Trigger detection: in ldo/orchestrator.py (or an escalation entrypoint), after the panel/tensor is built, for edges/series flagged by detect_structural_break run interrupted_time_series at the detected shock_index; also accept caller-supplied dated shocks (epidemic onset, policy dates) so ITS can be invoked on the §VII.1 'Escalate' verb over a named subgraph. Emit ITSResult (level_change β2, slope_change β3, and t-stats) as a Rung-2-tagged causal claim carrying its assumptions, per §IV 'every causal claim MUST carry its rung and assumptions'. (2) DiD dispatch: expose difference_in_differences through the escalation interface so 'estimate the effect of <shock> on <treated> vs <control>' can supply treated/control series and pre/post masks, returning a Rung-2 claim. (3) Implement the missing third tool — negative-control outcomes: add a function that, given a candidate exposure and a set of outcomes known to be causally unrelated to it, tests whether the same ITS/DiD design spuriously 'detects' an effect on the negative-control outcome; a nonzero effect there flags residual confounding/bias in the design and downgrades or vetoes the primary Rung-2 claim. Add one focused proof-of-capability test (per the 'prefer architecture over tests' directive): a planted dated shock recovered by ITS on the real panel path, and a negative-control outcome correctly showing no effect. Route all of this so it is reachable from run_ldo/run_investigate or the on-demand 'Escalate' verb, mirroring how orient_links (Rung-1) is already invoked in orchestrator.py.

**MAJ-11** [present_divergent] — §V.6(2) (l.300)  — _Status: PENDING_
- REQ: §V.6(2) Propagate approximation error into results: numerical (randomized-SVD / stochastic-logdet) error becomes a component of each affected edge's uncertainty (widened error bars); never present an approximated number as exact.
- FIX: Add a numerical-approximation error term to each affected edge's `uncertainty` and expose it as a control signal. Concretely: (1) In _low_rank_factors (lowrank.py:65-87) return a randomized-SVD readout-error estimate alongside factors — e.g. HMT a-posteriori bound on ||L - U diag(s) U^T|| from the (rank_cap+1)-th probed singular value, or a power-iteration residual norm; propagate a per-factor loading-error scale from it. (2) For latent_shared edges (built from factor loadings in lowrank.py:182-204 / edges.py:183-194), set uncertainty to this loading-perturbation bound instead of leaving it None, so a shared-driver flag read off a randomized factor carries its readout error. (3) When the R-step logdet is computed by stochastic Lanczos quadrature/Hutchinson (per §V.3 — currently a full eigh, so add the SLQ path with probe-count-controlled variance), carry the logdet variance into the precision-entry uncertainty for direct edges, combined in quadrature with the existing stability/statistical term. (4) Emit a per-edge diagnostic (e.g. `approx_error` component and an `approximation_limited` flag) so the §V.5 Adaptive Precision Controller can escalate a decision-relevant edge whose numerical term dominates to exact computation, rather than only the current all-or-nothing descriptive downgrade. Keep the existing noise-floor drop and convergence gate; they are complementary, not a substitute for the per-edge error bar.

**MAJ-12** [orphaned] — §VIII.1/§III.3 multi-resolution; §V.1 tiling; resolution.py docstring  — _Status: PENDING_
- REQ: Coarse->fine multi-resolution scanning as the live discovery+laptop-scaling strategy (each pass envelope-sized; candidate edges refined fine).
- FIX: Full RES-01/EXH-01/SCALE-01 (Phase 7) build; the orphaned resolution.py covers <20% of it and its selection-based candidate filter must be replaced, not merely called. Concretely: (1) Replace _candidate_pairs' certification-selected filter with a §VIII.2(1) sensitivity screen: compute per-coarse-cell subgroup heterogeneity / dispersion / max-subgroup statistics (a cancellation edge has ~zero pooled mean but high heterogeneity) and promote a pair to 'candidate' when ANY of those fire, tuned for recall, not when the coarse pooled edge was selected. (2) Make the coarse->fine step a readout of the §III.3 multiresolution posterior — i.e. build MR-01 (space/time/disease hierarchical GMRF/BYM shrinkage) first and drive fine refinement from the posterior (unsupported fine detail shrunk to parent), rather than the current naive refit-on-restricted-variables. (3) Add §VIII.2(2) random deep audits: on a random sample of pruned branches, run the full fine analysis and record the empirical false-negative rate. (4) Emit the §VIII.2(3) typed coverage manifest: for every region NOT searched, a typed 'unsearched' record (resolutions/conditioning-sets/functional-forms covered, where pruned and why), with the sparsity-of-truth assumption stated explicitly. (5) Under §V.1/SCALE-01, make each pass envelope-sized via tiling/streaming and refuse (scale_exceeds_compute_envelope) rather than degrade. (6) Then wire the result into run_investigate/compile so the live national path actually executes it. Also fix the resolution.py docstring to cite MSD-III §VIII.1/§VIII.2/§III.3, not MSD-II §II.7.

**MAJ-13** [missing] — §V.6(2) (l.300)  — _Status: PENDING_
- REQ: Propagate approximation error into results: numerical (approximation) error MUST become a component of each affected edge's uncertainty (widened error bars); never present an approximated number as exact.
- FIX: Fold numerical (approximation) error into each affected edge's LinkRecord.uncertainty as an additional variance component, and combine it with statistical uncertainty in quadrature (reuse Quantity.total_uncertainty = hypot(statistical, numerical) from compute/controller.py so the two subsystems share one definition).

Concretely:
1. In fit_sparse_plus_lowrank (lowrank.py), when the randomized-SVD path is taken, compute a numerical-error estimate for the recovered L (e.g. the HMT spectral tail bound / the discarded singular-value mass past rank_cap, or a one-shot exact-vs-randomized residual on the top block) and return it on SparseLowRankFit (new field, e.g. `lowrank_numerical_error`). When ADMM stops unconverged, return the final primal residual ‖primal‖/‖R‖ as a numerical-error magnitude rather than only the boolean.
2. Propagate that per-fit numerical error to the edges read off S/L: in to_link_records (and the latent_shared/residual readouts), map the fit's numerical error onto each affected edge's `uncertainty` (widen it), combining with any existing statistical uncertainty in quadrature. Backbone edges that currently carry uncertainty=None must carry at least the numerical component when an approximation was used.
3. Have certify.py treat `uncertainty` as the combined bar: an edge whose numerical component dominates and whose widened bar crosses the edge_threshold must NOT be certified "selected" (either downgrade to descriptive or route it through the §V.5 controller for exact escalation). Keep the existing unconverged→descriptive gate but generalize it so a converged-yet-randomized readout is not silently exact.
4. Wire the §V.5 controller: build Quantity objects (statistical_uncertainty from stability/holdout, numerical_uncertainty from step 1) for decision-relevant edges and run adaptive_precision_run so a numerically-dominated edge escalates to the exact eigh path (randomized_factors=False) — the self-correcting §V.6(2)/§V.5 coupling — instead of the controller remaining orphaned.
5. Complete the §V.6(3) partner check (exact state-scale certifies national approximate within the propagated bound) so the widened bars are validated, not merely emitted.

**MAJ-14** [missing] — §V.3 (l.280)  — _Status: PENDING_
- REQ: Stochastic log-determinant (Hutchinson + stochastic Lanczos quadrature) for the Gaussian likelihood log-det — unbiased, variance controlled by probe count (as in GPyTorch).
- FIX: Two spec-faithful directions, in priority order. (1) Provide the §V.3 estimator where the spec actually places it — as an anytime, probe-count-controlled logdet EVALUATOR feeding the §V.5 Adaptive Precision Controller and the §V.6 validity report, not as a replacement for the ADMM prox. Add stochastic_logdet(matvec, n_probes, lanczos_steps, seed) in a new ldo module: draw Rademacher/Gaussian probe vectors z, run k-step Lanczos on the matrix-free operator Ω_var = S − L (reuse the existing matrix-free matvec infrastructure in covariance.py) to build the tridiagonal T per probe, and estimate logdet(Ω_var) = (p/m) Σ_probes e1^T f(T) e1 with f = log via the Gauss quadrature nodes/weights of T (eigendecompose the small k×k T). Return the estimate plus its Monte-Carlo standard error (variance ∝ 1/m) so the controller has the prescribed probe-count variance knob. Wire this into the final likelihood/validity reporting on Ω_var and expose n_probes as a scheduler dial per §V.5. (2) Only if the ADMM R-step eigh itself is shown to be the compute bottleneck at national p: keep the eigh for the prox (it is mathematically required to reshape the spectrum) but exploit that Ω_var is sparse-plus-lowrank — the prox eigendecomposition can be accelerated via the low-rank structure of L (S is sparse; the dense cost comes from L's rank-r update), or bound p via Kronecker per-axis factorization (§V.2, l.275) so each per-axis eigh is O(p_axis^3) rather than O((p·S·T)^3). Do NOT attempt to swap SLQ in for the prox — that is a category error (SLQ gives a scalar logdet, the prox needs the eigenbasis). Gate either build behind a proof-of-capability test: SLQ estimate must match np.linalg.slogdet(Ω_var) within the reported SE band on a synthetic sparse-plus-lowrank Ω at national-scale p, demonstrating the unbiasedness and probe-count variance decay §V.3 requires.

**MAJ-15** [present_divergent] — §V.1 (l.268) + §V.4 (l.287)  — _Status: PENDING_
- REQ: Mixed precision: float32 bulk; float64 (or Kahan) on reductions/log-dets/ill-conditioned solves; MONITOR condition numbers and ESCALATE to float64 when they exceed threshold.
- FIX: Two coordinated fixes on the LDO fit path:

1. Honor float32 bulk OR correct the envelope model. Cleanest spec-faithful option: keep the dense LVGLASSO/covariance/lag bulk in float32 with float64 promotion only at reductions/log-dets. Concretely: build the lagged sample matrix (lags.py:62) and the empirical covariance accumulators (covariance.py G/R, the emp_cov fed to fit_sparse_plus_lowrank) in float32; in lowrank.py's ADMM keep the S/L/U/R iterates float32 but do the trace/logdet reductions and the eigh in _prox_neg_logdet / _psd_project_shifted in float64 (cast the symmetric matrix up before eigh, cast the result back), and accumulate the primal-residual norm (l.151) in float64 or via Kahan. If float32-everywhere is deemed too risky numerically, then at minimum fix estimate_ldo_bytes (envelope.py:53-64) to use 8 bytes for the float64 'samples' and 'cov' terms (or a per-term dtype map) so assert_within_envelope stops under-counting peak memory by ~2x — the current guard can green-light an OOM run.

2. Add the condition-number monitor + escalate gate (§V.4). Before/at the ill-conditioned solves — the graphical_lasso input corr in precision.py:121, the emp_cov in fit_sparse_plus_lowrank (lowrank.py:126 C), and the eigh calls — compute a cheap conditioning proxy (e.g. ratio of max/min eigenvalue from the eigh already being done, or np.linalg.cond on the p x p precision block) and, when it exceeds a configured threshold, promote that specific solve/reduction to float64 (and, per §V.6, widen the affected edge's uncertainty / emit a typed diagnostic). Record the observed condition numbers and any escalations in run_ldo's diagnostics dict (orchestrator.py:194) so the behavior is auditable rather than silent. This makes precision adaptive-by-conditioning as the spec requires instead of a static unconditional float64.

**MAJ-16** [present_divergent] — §V.2 (l.275)  — _Status: PENDING_
- REQ: Kronecker separability: matvec/solve/log-det factor across the small per-axis factors (Omega_var, Sigma_space, Sigma_time), collapsing a (p*S*T)^2 object into p^2/S^2/T^2 pieces.
- FIX: Introduce a genuine Kronecker-factored operator for the joint precision instead of dense-eigh-on-lag-stacked-features. (a) Represent the model as Ω_var ⊗ Σ_space^{-1} ⊗ Σ_time^{-1} with three small factors: p×p variable precision (LVGLASSO), the sparse S×S GMRF Σ_space^{-1}=κI+L_W (already in precision.build_spatial_precision_sparse), and a NEW (K+1)×(K+1) temporal factor Σ_time^{-1} (AR/temporal-smoothness precision) replacing pure lag-stacking. (b) Add factored primitives (new ldo/kron.py): kron_matvec via the vec-trick generalized to 3 factors by reshaping to a (p,S,T) tensor and contracting one axis at a time (never materializing the (pST)² operator); factored log-det = S·T·logdet(Ω_var) + p·T·logdet(Σ_space^{-1}) + p·S·logdet(Σ_time^{-1}), each on the small factor (the S-factor via sparse Cholesky); matrix-free solve by CG/MINRES using kron_matvec plus a Kronecker-factor preconditioner (referenced in §V.4 l.286). (c) Wire the factored log-det/solve into the ADMM R-step (currently _prox_neg_logdet dense eigh) so -logdet(S−L) uses the factored operator, per the GPyTorch precedent named in §V.2. (d) Keep the whitening split as the small-scale fallback, make the factored path the one satisfying the (pST)²→p²+S²+T² collapse, and gate it under the §V.6 validity contract (exact-certifies-approximate) since §III.4(3) marks separability an approximation.

**MAJ-17** [present_divergent] — §V.1 (l.268-270)  — _Status: PENDING_
- REQ: Compute-envelope refusal: a run that cannot fit MUST refuse with scale_exceeds_compute_envelope, never silently subsample; the dense-exceeds form must be blocked/refused, never OOM.
- FIX: Fix the fit-path byte model so the guard reflects the run it protects. Two spec-faithful options, in order of preference:\n\n(1) Make the LDO fit honor the §V.1 float32-bulk mandate — build the lag-extended sample matrix (lags.py:62) and the extended-covariance/precision working set in float32, keeping float64 only for reductions/log-dets/ill-conditioned solves per §V.4 (mixed precision). Then _DTYPE_BYTES=4 in estimate_ldo_bytes becomes truthful and the guard is faithful. This also brings the fit into §V.1 compliance, not just the guard.\n\n(2) If float64 bulk must be retained for numerical fidelity, set _DTYPE_BYTES=8 (or split the estimate: float64 for the samples/cov terms, float32 only where genuinely float32) so estimate_ldo_bytes >= the true peak allocation. Update the docstring away from \"All in float32.\"\n\nAdditionally (defense-in-depth, optional): hoist the residual-scan RAM check into assert_within_envelope (or add a sibling assertion at orchestrator.py before the scan) so a single envelope call gates both the fit and the scan, and consider narrowing the orchestrator.py:164 except to not swallow ScaleExceedsEnvelopeError silently — re-raise or emit it as an explicit typed 'scale_exceeds' diagnostic rather than a generic residual-scan error string, so the refusal is loud per §V.6.

**MAJ-18** [missing] — §V.6(3) (l.301)  — _Status: PENDING_
- REQ: Exact certifies approximate: a state-scale exact run MUST agree (within propagated bounds) with the national approximate run where they overlap; disagreement beyond bounds rejects the approximation loudly.
- FIX: Implement the §V.6(3) cross-scale certification as a live gate, wired so it actually runs on the investigate path. Concretely: (1) Add validation/holdout.py (the plan's designated home, l.152) with a comparator, e.g. certify_exact_vs_approx(national_run: LDORun, state_exact_run: LDORun, state_uf: str) that (a) restricts both runs to the overlapping edge set = edges whose endpoint variables are present in the state UF's scope, (b) for each overlapping edge compares the national approximate estimate against the state exact estimate using the edge's PROPAGATED uncertainty (the §V.6(2) widened error bars already carried on LinkRecord.uncertainty) as the bound — flag disagreement when |approx - exact| exceeds a k-sigma multiple of the combined propagated uncertainty, and (c) emit a typed rejection (raise/loud warning, not silent) when any decision-relevant edge disagrees beyond bounds. (2) Produce the state-exact reference run by re-invoking run_ldo with enforce_envelope forcing the exact path (randomized_factors=False, exact logdet, no sketching) on the state-scoped panel — the compute envelope is affordable at single-UF scale by construction. (3) Wire it: after the national approximate run_ldo completes on the investigate path (workflows/investigate.py run_investigate / compile.py l.940-942), select at least one representative UF, build its state-scoped exact run, call the comparator, and thread the certification verdict into the run diagnostics/output so a failed cross-scale check downgrades or rejects the affected national edges loudly rather than presenting them as certified. (4) Add the VAL-04 acceptance test (tests) that plants a national structure, runs approximate national vs exact state, and asserts the gate passes on agreement and rejects loudly on an injected bound-violating perturbation.

**MAJ-19** [orphaned] — §III.5; §I.2; §II.3  — _Status: PENDING_
- REQ: Copula margins: extensive quantities MUST use a count-with-exposure margin (Poisson-offset / binomial), so dependence is modelled net of exposure — normalization lives in the model as an offset, never as a pre-materialized rate (the denominator principle).
- FIX: Wire exposure end-to-end into compile→panel→LDO so extensive/flow variables hit the Poisson-offset margin. (A) she/panel.py compile_common_panel: for each field whose EFG output has a sibling <field_id>.measured_quantity.parquet (or whose V_fields entry carries measured_quantity_ref/exposure_ref and offset_semantics=="log_exposure"), load that sidecar and scatter its `exposure` column onto a new (p,S,T) exposure tensor E aligned exactly like X (same municipality_cod6×year index, NaN where absent); also swap the field's modeling value from the pre-divided rate to numerator_count for those fields so X carries the count and E the denominator (satisfies §I.2). Store E on CommonPanel (panel.exposure) or return it from assemble. (B) assemble.py assemble_ldo_tensor: build/carry the aligned exposure tensor as LDOField.exposure (variables without a declared exposure_ref stay NaN → rank-PIT). (C) orchestrator._prepare_ldo_inputs/run_ldo: source `exposure` from the LDOField/CommonPanel when the caller didn't pass one (mirroring disease_graph/variable_meta auto-derivation) instead of defaulting to None. (D) workflows/investigate.py run_investigate: derive the exposure tensor for keep_variables from V_fields measured_quantity_ref (analogous to disease_variable_meta) and pass exposure=... into run_ldo; add diagnostic n_exposure_margins. Guard: only route to count_exposure_gaussianize when the registry tags kind==flow or extensive==true AND positive exposure exists (§I.1-I.2), so intensive fields stay on the rank margin. Add one proof-of-capability test: a compiled run with a count field + its exposure sidecar yields diagnostics.n_exposure_margins>0 and the extensive variable's latent Z is invariant to a size-only rescaling of exposure at fixed rate.

**MAJ-20** [orphaned] — §III.3; §I.5; §VIII; XI.3 (RES-01)  — _Status: PENDING_
- REQ: Multiresolution hierarchical shrinkage / coarse->fine readout: resolution is first-class; the scanner is a readout of the multiresolution posterior (coarse discovery then fine refinement of candidate edges), not a single fixed grain.
- FIX: Two coordinated builds, spec-faithful: (1) MR-01 substrate (§III.3): build the multiresolution hierarchical-shrinkage prior so the latent field is a sum-of-components across nested scales on each hierarchical axis (space: national+regional+state+municipal deviations; time: year+quarter+month; disease: chapter+block+category+leaf), with a GMRF/BYM prior shrinking each fine level toward its coarse parent (INLA-over-latent-GMRF / SPDE style). This is the posterior the scanner must read; today only a flat single-scale (kI+L_W) whitening exists in ldo/lags.py. Assemble the nested parent-child incidence/aggregation operators from the existing hierarchy registries (spatial cod6 adjacency already in precision.py; disease DiseaseGraph L_D; a time-nesting map) and add the cross-scale shrinkage term to the precision estimate in lags.py/precision.py. (2) RES-01 wiring: change the live path in src/pegasus/workflows/investigate.py to call run_multiresolution_ldo (already built, resolution.py:64) instead of run_ldo (line 179), passing a coarse_field and fine_field derived from the compiled panel at two grains (e.g. state x year coarse, municipality x month fine) rather than the single resolution='year' compile at line 141/158. Per §VIII.1/§VIII, the coarse screen MUST fire on heterogeneity/dispersion/max-subgroup signal (not the pooled mean) to avoid Simpson-reversal/cancellation false negatives — so _candidate_pairs/the coarse selection in resolution.py should screen on a sensitivity statistic, and a coverage/sparsity-of-truth manifest should record the assumption. Emit cross-scale consistency (a coarse candidate edge sharpened at fine grain) in the merged LinkRecords/diagnostics. Gate with one proof-of-capability test: a planted edge visible only at fine grain (or a cancellation case with zero coarse mean but high heterogeneity) is recovered by the multiresolution path but missed by single-pass run_ldo.

**MAJ-21** [present_divergent] — §III.7; §III.8; §V.6(2)  — _Status: PENDING_
- REQ: Link record uncertainty: every promoted edge must carry propagated uncertainty; certification requires holdout stability AND propagated uncertainty; approximation/numerical error must widen the affected edge's uncertainty (never present an approximated number as exact).
- FIX: Two spec-faithful fixes. (1) Make promotion a true conjunction for the linear backbone: in edges.py to_link_records, populate LinkRecord.uncertainty for lagged_directed and contemporaneous edges with a propagated uncertainty term (e.g. a partial-correlation standard error from the fitted precision / n_eff, or a stability-selection dispersion), and in certify.py require BOTH stability >= min_stability AND uncertainty is not None before status='selected' for these types (add the uncertainty check to assert_ldo_edge_promotion_allowed's lagged/contemporaneous branch, matching the nonlinear_residual branch at certify.py:75-77). (2) Realize §V.6(2) numerical-error propagation: thread the bounded approximation errors into each affected edge's uncertainty via root-sum-square with the statistical term -- the randomized-SVD tail bound (Halko-Martinsson-Tropp residual from lowrank.py rSVD) for edges read off the low-rank / latent_shared layer, the ADMM primal/dual residual at termination (lags.py fit) as a numerical-tolerance component for all backbone edges, and the whitening-imputation variance where cells were imputed. Expose these per-edge error components in diagnostics so the Adaptive Precision Controller (§V.5) can escalate approximation-limited edges to exact. Keep the existing convergence gate as the hard-fail fallback, but it must supplement, not replace, per-edge error-bar widening. For nonlinear_residual edges, also carry the hsic.py residual_uncertainty (bootstrap-null sd) as the uncertainty rather than overloading the q-value.

**MAJ-22** [orphaned] — §III.8 (standing aborts)  — _Status: PENDING_
- REQ: Standing abort (§III.8, extends MSD-I §10): an edge promoted without holdout stability or propagated uncertainty MUST hard-fail; likewise reconstructed/nonlinear edges without propagated uncertainty.
- FIX: Wire the standing abort as a final backstop over every record on the live path, matching §III.8's "refuse" semantics rather than relying solely on certify_link's soft downgrade. Concretely: in run_ldo (orchestrator.py), immediately after the certify_links(...) call at line 179 (and after orient_links at :191, since orientation may reorder source/target), add: `for r in records: assert_ldo_edge_promotion_allowed(r)` (import it from pegasus.ldo.certify). This makes any 'selected' lagged/contemporaneous edge with stability is None, or any 'selected' nonlinear_residual edge with uncertainty is None, raise LDOCertificationError and refuse the run — the mandated hard-fail — so a future path that constructs a selected record bypassing certify_link, or a regression in certify_link, cannot silently emit an uncertified edge into Hypotheses. Additionally extend the check to cover the spec's second limb ("reconstructed tensor promoted to verified without holdout/uncertainty"): assert_ldo_edge_promotion_allowed currently only checks lagged_directed/contemporaneous/nonlinear_residual and has no clause for reconstructed/verified edges, so add that abort condition to the function before wiring it. Add one focused test that feeds run_ldo (or write_hypotheses via the investigate path) a hand-built selected record with stability=None and asserts LDOCertificationError is raised.

**MAJ-23** [present_divergent] — §III.7 (LinkRecord); §VI/§II.8 output bundle  — _Status: PENDING_
- REQ: Output payload: the Hypotheses key carries typed LinkRecords with all prescribed fields (edge_type, lag_k, weight, partial_correlation, response_curve_ref, spatial_field_ref, stability, uncertainty, confounding_factor_refs, projection_status, code_system, topology_role, overlap_jaccard, certification_status, warnings).
- FIX: Populate spatial_field_ref for each certified edge with a per-edge spatial-heterogeneity field that answers "where is this link strongest" (§X.1). Concretely: (1) In the lag-extended estimator (fit_lagged_links / lags.py), fit the edge (i,j,lag_k) with a spatially varying coefficient rather than a single scalar partial correlation — i.e. estimate a coefficient field beta_ij(s) over the S places, regularized by the spatial GMRF L_W (kappa*I + L_W) already available in fit_kwargs (this is exactly the "effect-modification enters as varying-coefficient structure" of §III.4 clause 5). (2) Persist that S-vector (or its sparse/low-rank summary) as a side asset (e.g. spatial_fields/{edge_key}.parquet keyed by muni/UF code) and set spatial_field_ref on the LinkRecord to that asset id/path, mirroring how response_curve_ref serializes the distributed-lag profile. (3) Thread it through to_link_records so all four edge types can carry it (contemporaneous and lagged at minimum; latent_shared/nonlinear_residual may legitimately leave it None). Minimum viable version if full varying-coefficient fitting is out of scope now: compute per-place (or per-UF) edge strength by refitting/evaluating the pairwise partial correlation within each spatial block of L_W, store the block-wise vector as the spatial field, and reference it — this satisfies the §XI.4 "spatially-heterogeneous" and §X.1 "where is this link strongest" capability without the full GMRF-smoothed estimator.

**MAJ-24** [present_divergent] — §3.12 / §3.12.3 (referenced by §II.3 'uncertainty(state tensor)')  — _Status: PENDING_
- REQ: Q-tensor is the state-diagnostic (§3.12): per-field n_eff via Kish effective sample size (Σw)²/Σw² deflated by 1/(1+max(0,MoranI)), missingness, cv, moran_i, temporal roughness, spatial entropy, provenance risk, feeding the verified/fragile/quarantined classification.
- FIX: Make compute_q_state the single live producer of the Q_tensor and retire the parallel _vector_diagnostics classification/weighting drift. Concretely: (1) In compile_attach._q_row, replace the _vector_diagnostics dict + field.state passthrough with a call to q_tensor.compute_q_state(field=field, tensor=vector, denominator=<aligned denominator vector for rate fields>, provenance=field.provenance, warnings=field.warnings), and serialize the returned QState into the Q_tensor row (use its fields directly rather than hand-rolling the dict). This routes the denominator-preferred Kish weighting (§3.12.3) and the graded provenance_risk ladder onto the live path. (2) Drive the field's verified/fragile/quarantined 'state' from classify_q_state applied to the live-computed n_eff/denom_fragility/missingness/risk, instead of materialize.py:225's missing_rate>0.5 heuristic — either write QState.state into the Q_tensor row, or have the panel carry the QState so the state taxonomy reflects the §3.12.3 thresholds. (3) Move the panel-aware _temporal_roughness/_spatial_entropy (compile_attach.py:83,98) into q_tensor as the canonical implementations (or have compute_q_state accept the panel), so there is exactly one implementation of every §3.12 field. Add a contract test asserting the Q_tensor 'state' column equals classify_q_state(n_eff,...) on a fixture with n_eff below 100 but missing_rate below 0.5 (a case where the two classifiers currently disagree).

**MAJ-25** [stubbed] — §II.3 (line 129 'uncertainty(state tensor)')  — _Status: PENDING_
- REQ: The MeasuredQuantity uncertainty field is the state tensor (per-cell state / denom_fragility / propagated uncertainty), so a normalized quantity carries its state into the LDO.
- FIX: Attach the full §3.12 state tensor to the MeasuredQuantity instead of a lone scalar, and consume it in the LDO. Concretely: (1) In measured_quantity_from_rn_join (measured_quantity.py:62-91), replace the uncertainty={'denom_fragility': ...} literal with the field's computed QState — either pass the QState/dict from the RN kernel (executor/kernels.py:393) or compute it there via q_tensor.compute_q_state, serializing all 14 components (n_events, n_denom, n_eff, cov_S, cov_T, missingness, zero_inflation, denom_fragility, cv, moran_i, temporal_roughness, spatial_entropy, provenance_risk, state). The kernel already computes num_total/den_total (n_events/n_denom) and combined_fragility, so most inputs are in hand at the call site. (2) Persist the state tensor alongside the count+exposure table (either an extra metadata sidecar or per-cell columns if a per-cell tensor is intended) so write_measured_quantity round-trips it. (3) Wire it into the model: in the LDO orchestrator/estimator (ldo/), read the state tensor for each quantity and use it as the per-quantity precision/observation-weight (e.g. n_eff and denom_fragility down-weight fragile cells; provenance_risk and missingness widen the propagated edge uncertainty) so a normalized quantity actually carries its state into the fit rather than being modeled as if exact. Guard with one focused test: an RN-produced MeasuredQuantity's uncertainty must contain the full §3.12 component set and a high-denom_fragility / low-n_eff field must measurably widen its edge uncertainty on the run_ldo path.

### MINOR (10)

**MIN-1** [missing] — §V.6(3); Appendix F.3 (invokes 'exact-certifies-approximate')  — _Status: PENDING_
- REQ: Exact-certifies-approximate (§V.6.3): a state-scale EXACT CPW run must agree, within propagated bounds, with the national APPROXIMATE (randomized-SVD) run where they overlap; disagreement beyond bounds rejects the approximation loudly
- FIX: Implement a two-run CPW reconciliation on the live path, honoring §V.6(2)+(3): (1) Add a certifier, e.g. pegasus/ldo/exact_certify.py, that takes the national APPROXIMATE fit and, on the state-scale overlap (the subset of variables/edges present in a state-scoped slice), performs an independent EXACT CPW fit — fit_sparse_plus_lowrank called with randomized_factors=False (forcing the dense eigh readout) AND, critically, on state-scale data so it is a genuine separate run rather than a re-read of the same L. (2) Compare the overlapping quantities: latent_shared loadings / factor_values and direct-edge partial correlations, computing the discrepancy against the propagated randomized-SVD error bound (Halko-Martinsson-Tropp spectral-norm bound for the n_iter/n_oversamples used at that rank_cap). (3) If any overlapping edge disagrees beyond its propagated bound, reject loudly — raise a typed error (mirroring ScaleExceedsEnvelopeError) or emit a coverage-manifest 'approximation_rejected' flag — never silently keep the approximate edge. (4) Per §V.6(2), fold the randomized-SVD error estimate into each approximate latent_shared edge's uncertainty field (widened error bars) inside run_ldo's readout, so LinkRecords carry propagated numerical uncertainty even when the certification run is not available. (5) Wire the certifier into run_ldo / run_investigate (or the standing VAL-04 validation battery) so the national approximate path is actually certified where a state-scale overlap exists, and mark uncertified overlaps as approximation-limited (typed, not silent) per §V.5. Finally, correct the overclaiming docstrings in lowrank.py (lines 74-75 and 170-173) so they no longer assert §V.6 exact-certifies-approximate is satisfied by the intra-fit readout agreement.

**MIN-2** [missing] — §V.6(2)(3)  — _Status: PENDING_
- REQ: §V.6: an exact (state-scale) run must certify the national approximate whitened result within propagated bounds (exact-certifies-approximate); the Lanczos-sqrt / matrix-free approximation error should propagate into edge uncertainty.
- FIX: Make the whitening operator honor §V.6(2)+(3): (a) Have _sqrt_matvec return an error estimate alongside h — the standard Lanczos-function-approximation a-posteriori residual (e.g. beta_{m}*|e_m^T f(T_m) e_1| from the (m+1)-th Lanczos coefficient, or a two-estimate m vs 2m difference ||h_2m - h_m||) — and thread that scalar centering-error bound out of whitened_lagged_correlation on PairwiseCovariance (add an approx_error field). (b) Propagate it: convert the centering-error bound into a per-edge widening of the whitened correlation (delta-method on corr = Gk / outer(d,d) given the bounded perturbation to H, hence to G) and fold it into LinkRecord.uncertainty for whitening-affected edges, so a whitening-approximation-dominated edge widens rather than reads as exact — mirroring the existing convergence-gate downgrade pattern in orchestrator.py:169-177. (c) Wire an exact-certifies-approximate check for the whitening: on a state-scale slice (small S) compute the exact centering h_exact via a dense (kappa*I + L_W)^{1/2} (or a much larger m), compare against the m=40 national-style approximate h on the overlapping municipalities, and assert agreement within the propagated bound — failing loudly (downgrade edges to descriptive) when the m=40 truncation error exceeds the certified envelope, exactly as §V.6(3) requires. Emit the whitening approximation-error term into orchestrator diagnostics (currently orchestrator.py:194-211 carry none).

**MIN-3** [present_divergent] — §III.7  — _Status: PENDING_
- REQ: Stability subsampling should perturb the lattice (§III.7 'lattice subsamples'); subsample count and selection threshold faithful.
- FIX: Broaden the stability perturbation in edges.py:stability_select to cover the full lattice and tuning axis, per §III.7 + §IX.3:

1. Temporal (lattice) perturbation. Add a temporal subsample alongside the spatial one: for each of the n_subsamples draws, also draw a contiguous or random time-window subset t_idx of size round(frac*T) (contiguous block-bootstrap is preferable for lagged edges so lag structure within the window is preserved), and build the subsample field with time_ids=[time_ids[j] for j in t_idx], Z=field.Z[:, s_idx][:, :, t_idx], W likewise. This makes each refit a genuine Place x Time lattice subsample. Guard T so that the retained window still satisfies T_sub > K (fit_lagged_links requires T > K); if frac*T would drop below K+2, clamp the temporal frac up (or skip temporal perturbation for that draw and record it) rather than producing an unfittable window.

2. Tuning perturbation (§IX.3). Add a small jitter of the regularization operating point across subsamples — e.g. draw lambda1 (and optionally lambda2) per subsample from a bounded grid around the base value (say {0.5x, 1x, 1.5x}) and pass it through fit_kwargs into _subsample_edges. An edge that only survives at one lambda is fragile; requiring recurrence across both data subsamples and tuning jitter is exactly the "survive ... tuning perturbation" the spec demands.

3. Frequency accounting is unchanged: keep the existing counts/runs frequency and the lag_tolerance merge (edges.py:97-121); the only change is that the denominator now spans space x time x tuning perturbations. Thread the new knobs (temporal_frac, lambda_jitter grid) up through run_ldo with spec-defensible defaults, and record in diagnostics which axes were perturbed so a spatial-only fallback (when T is too short for a temporal window) is honest rather than silent.

**MIN-4** [missing] — §IV line 256; line 485  — _Status: PENDING_
- REQ: Rung 3 — interventional identification / do-calculus E[Y|do(X)], expert-invoked only; no autonomous Rung-3.
- FIX: Optional (not a blocker). If an expert-invocable Rung-3 entrypoint is desired, add pegasus/causal/identify.py providing a human-called (never auto-invoked from the LDO orchestrator) API that takes a user-supplied DAG/ADMG plus treatment X and outcome Y, runs Pearl's ID algorithm (or at minimum a backdoor/frontdoor-adjustment check) to test identifiability, and — when identified — computes the adjustment estimand E[Y|do(X)] with propagated uncertainty and an explicit rung="rung_3_interventional" plus assumptions label per §IV's requirement that every causal claim carry its rung and assumptions. It must stay gated behind an explicit expert call site (no invocation from ldo/orchestrator.py), preserving §XII line 485's "no autonomous Rung-3", and must refuse (return non-identifiable, typed) rather than fabricate an effect when the graph does not license identification.

**MIN-5** [present_divergent] — §IV line 258; line 485 'every causal claim carries its rung'  — _Status: PENDING_
- REQ: Every causal claim MUST carry its rung and assumptions; strength explicit and never overstated (typed ladder semantics).
- FIX: Add two first-class fields to LinkRecord (src/pegasus/ldo/records.py) and thread them through as_row / LINK_RECORD_COLUMNS: (1) `rung: int` in {0,1,2,3} (default 0 — the LDO baseline is Rung 0, time-directed only); (2) `assumptions: tuple[str, ...] = ()` naming the licence(s) invoked (e.g. 'time_precedence', 'lingam_non_gaussian_disturbance', 'faithfulness_collider', 'gaussian_unidentifiable'). Optionally add `orientation_confidence: float | None` as a typed field rather than a formatted string in warnings, so strength is machine-readable and never re-parsed from text. In src/pegasus/causal/orient.py:orient_edge, set rung=0 with assumptions=('time_precedence',) for lagged edges; rung=1 with assumptions=('lingam_non_gaussian_disturbance',) and orientation_confidence=confidence when a LiNGAM direction is licensed; rung=1 with assumptions=('gaussian_unidentifiable',) and no direction when left undirected. Keep the warnings tokens for backward compat but make rung/assumptions the authoritative fields the diagnostics and downstream consumers read. To fully honor the ladder, wire the Rung-2 quasi.py results (ITSResult/DiDResult) onto the corresponding LinkRecord (or emit them as rung=2 records with their design assumptions, e.g. ('its_parallel_no_cotemporal_shock',) / ('did_parallel_trends',)) rather than leaving them free-floating, so 'every causal claim carries its rung and assumptions' holds for Rung 2 as well as Rung 1. No change to the (already conservative) identifiability math is required; this is a schema/typing fix, not a numerics fix.

**MIN-6** [orphaned] — §III (l.245); certify.py docstring l.9-10  — _Status: PENDING_
- REQ: §10 standing abort: a dependency edge promoted (selected) without holdout stability or propagated uncertainty must be refused loudly.
- FIX: Wire assert_ldo_edge_promotion_allowed as an actual post-certification gate on the live path. In orchestrator.py, immediately after line 179 (records = certify_links(...)) and after orientation (orient_links at l.191, since orientation could in principle re-type/re-promote), iterate every record and call assert_ldo_edge_promotion_allowed(r), letting LDOCertificationError propagate loudly (do NOT wrap in the additive try/except used for the residual scan — this must abort the run, per 'refuse-don't-degrade'). Import it from pegasus.ldo.certify alongside certify_links at l.23. This converts the invariant from 'true by construction, unchecked' to 'enforced as a standing gate', matching §III.8. Optionally strengthen the assert itself so the contemporaneous/lagged branch ALSO accepts propagated uncertainty as an alternative to stability (the spec says 'stability OR propagated uncertainty'), and so latent_shared/mechanical_overlap 'selected' promotions (if ever produced) are likewise validated, to fully cover the clause's 'edge promoted' scope rather than only the three currently-checked edge_types.

**MIN-7** [missing] — §V.3 (l.280)  — _Status: PENDING_
- REQ: §V.3 Stochastic log-determinant (Hutchinson + stochastic Lanczos quadrature) for the Gaussian likelihood, variance controlled by probe count.
- FIX: Two spec-faithful options, in priority order. (1) Preferred / minimal-honest: since the LDO precision object is bounded and the dense eigh is exact and cheap here, formally reclassify §V.3's stochastic-logdet as intentionally-exact for the LDO in Appendix F.3 (it already half-does this) AND close the real gap — implement the §V.6(2) approximation-error propagation so any residual numerical uncertainty on an edge is widened into its error bars, and expose an honest 'exact logdet' telemetry flag so §V.5's controller knows this dial is pinned to exact (target already met, zero probe budget needed). This satisfies 'bounded, not heuristic' by being exact. (2) If a genuine stochastic estimator is wanted per the literal clause (e.g. for a future national marginal-likelihood evaluation the controller can tighten): add ldo/randnla.py implementing hutchinson_logdet(matvec, n, probes) = mean over Rademacher/Gaussian probes z of zᵀ·f(A)·z with f=log via stochastic Lanczos quadrature (m Lanczos steps → tridiagonal T → Σ_k (e1ᵀ u_k)² log(θ_k) on T's eigenpairs), matrix-free on the sparse/Kronecker precision; return the estimate WITH its probe-count-scaled variance and wire that variance through §V.6(2) into affected edges' uncertainty; gate on §V.5 so the controller can raise probe count or exact-escalate per edge (§V.5's self-correcting property). Either way, task #36 ('Randomized NLA §V.3 for LDO low-rank + logdet') must be reopened for the logdet half — only the randomized-SVD low-rank-factor half was delivered.

**MIN-8** [missing] — §V.3 (l.281)  — _Status: PENDING_
- REQ: Sketching (Johnson-Lindenstrauss) for the neighborhood regressions — (1±ε) distortion guarantees.
- FIX: Implement the §V.3 neighborhood-regression sketch as a bounded-error backend for the precision estimate, since the current estimator does not even use the per-node regression form. Concretely: (1) Add a Meinshausen–Bühlmann-style neighborhood-regression path for Ω_var — for each variable i, regress its (GMRF-whitened) cell vector on all others; the sign/support of the coefficients gives the precision off-diagonal support, matching the sparse-precision target. (2) Sketch the row dimension (the S·T cells, or the whitened Krylov features) with a Johnson–Lindenstrauss random projection (Gaussian or SRHT/CountSketch) to k = O(ε⁻² log p) rows before the least-squares/lasso solve, so each neighborhood solve runs on the sketched design SΦ with a provable (1±ε) subspace-embedding guarantee on XᵀX / Xᵀy. Wire the sketch size k as an APC dial (§V.5) driven by target uncertainty, and propagate the JL (1±ε) distortion into each affected edge's uncertainty per §V.6.2 (widened bars, never presented as exact). Gate with a §V.6.3 exact-certifies-approximate test: a small-S dense graphical_lasso run must agree within the propagated JL bound with the sketched neighborhood-regression run. (3) Independently, close the §V.3 bullet-2 gap by replacing the full-eigh log-det in the ADMM R-step with Hutchinson + stochastic Lanczos quadrature if that computation is also to be certified randomized (the spec l.537 currently exempts it, so this is optional unless §V.3 is read as binding on all three bullets).

**MIN-9** [missing] — §V.4 (l.285)  — _Status: PENDING_
- REQ: Streaming sufficient statistics: XtX / Xty are p^2 regardless of row count; stream Parquet/Arrow (DuckDB) once to accumulate them so national data need not fit in RAM.
- FIX: Add a streaming sufficient-statistics accumulation path for the Gaussian-scale LDO covariance so the dense (p,S,T) tensor need not materialize. (1) Introduce an out-of-core estimator (e.g. covariance.streamed_sufficient_stats) that consumes the CommonPanel's Hive-partitioned Parquet directly via a lazy scan (polars scan_parquet or the existing storage/duckdb.query_arrow adapter, which already returns Arrow) and, in a single pass, accumulates the pairwise sums pairwise_correlation needs (overlap counts n_ab = M@Mᵀ, Sx = X0@Mᵀ, Sxx = (X0²)@Mᵀ, Cxy = X0@X0ᵀ) as F×F running accumulators, batching rows chunk-by-chunk so peak memory is O(p² + chunk·p), never O(S·T·p); the final correlation is computed from these p²-sized accumulators exactly as today, so numerically identical. (2) For the whitened lagged path, stream one S-slice-per-time-step (load feat_t, apply Q@Ftᵀ, accumulate G += Ft@(Q@Ftᵀ) and R += Ft) holding only an (F,S) block — mirroring covariance.py:181-187 but sourcing Ft from a per-time scan_parquet slice instead of in-RAM Zc. (3) Route run_ldo/_prepare_ldo_inputs to the streamed estimator when the source is a large CommonPanel or when the dense estimate would exceed the envelope, falling back to assemble_ldo_tensor only for small fields. (4) On the streamed path, bound the envelope guard by the p²/F² accumulator size rather than the S·T·p dense tensor so national scale passes instead of refusing. This is the SCALE-01 inference-scan streaming item and does not require touching the already-compliant data-plane scan_parquet views.

**MIN-10** [present_divergent] — §III.4(3); §V.2; Appendix F(2)  — _Status: PENDING_
- REQ: Kronecker/separable structure (Ω_var ⊗ Σ_space^-1 ⊗ Σ_time^-1) as the structural-decomposition axis-by-axis factoring (§III.4(3)).
- FIX: 
