# PegaSUS Modularization / Consolidation / API-Boundary Critique

Objective 2: the codebase has repeatedly proven to carry redundant, orphaned, or badly-architected code — a symptom of scattered logic with no centralized computation, no clear module/API boundaries, and no enforced data contracts, even where the mathematical architecture implies clean, bounded steps. This ledger captures the structural problems (lead's direct survey first; the `modularization` workflow appends full findings). **Full granularity — nothing compacted.**

---

## Part A — Lead's direct structural survey

### M1 · `MOD-DUP-MORAN` — HIGH — Moran's I / spatial autocorrelation is computed SIX different ways
- **detail:** Spatial autocorrelation (Moran's I) — a single well-defined statistic — is reimplemented across the tree: `q_tensor._moran_contiguity` (1-D cell-order chain, the garbage proxy of A20), `_moran_i_adjacency`, `_moran_i_for_group`, `_moran_diagnostic`, `_moran_proxy`, plus `_moran_corrected_n_eff`. At least 4 distinct estimators of the SAME quantity, some geography-aware and some not, with no shared implementation or contract. This is the clearest single example of scattered, uncentralized mathematical logic, and it directly produces a validity bug (A20: the effective-n correction uses the non-geographic one).
- **evidence:** `grep -roE "def _moran[a-z_]*"` → `_moran_proxy, _moran_i_for_group, _moran_i_adjacency, _moran_diagnostic, _moran_corrected_n_eff, _moran_contiguity`.
- **recommendation:** ONE `pegasus.geo.spatial_autocorrelation` module: `moran_i(values, weight_graph)` taking the canonical `SpatialWeightGraph`, plus `effective_n(values, graph)`. Every consumer (q-tensor, LDO SE, diagnostics) calls it. Deletes 5 near-duplicates and fixes A20/A21.

### M2 · `MOD-DUP-CLEAN` — MEDIUM — eight string-cleaning / hashing / table-reading helpers reimplemented
- **detail:** `_clean` ×4 (+ `_clean_text`, `_clean_str`, `_clean_or_none`, `_clean_code`), `_digits` ×4, `_stable_hash` ×4, `_read_table` ×5 — the same primitive utilities copy-pasted across `datasus/normalize/*`, `sidra/*`, and others. Each divergent copy is a latent silent-failure site (one `_clean` strips differently than another → inconsistent decode). The earlier modules report (T2.5) flagged this at the normalizer level; it is broader.
- **evidence:** duplication scan above.
- **recommendation:** a single `pegasus.core.text` (clean/digits/stable_hash) + `pegasus.core.io` (`read_table`) that all modules import; delete the copies. Pin one canonical behavior with a contract test.

### M3 · `MOD-LAYER-GEO` — MEDIUM — geo (foundational) imports denominators (higher layer): a layering inversion
- **detail:** `geo/migration_affinity.py:33` imports `denominators.population.migration.MigrationFlowReconstruction`. Geometry/spatial primitives should be a FOUNDATION that denominators depend on — not the reverse. This inverts the dependency layering and risks an import cycle (denominators already depends on geo).
- **evidence:** `geo/migration_affinity.py:33`.
- **recommendation:** move `migration_affinity` into `denominators` (it is a denominator-domain artifact) or invert so the migration reconstruction depends on a geo-level affinity primitive; enforce a layering rule (geo/core/measurement never import efg/ldo/denominators/workflows).

### M4 · `MOD-DUP-ILR` — LOW/MEDIUM — ILR composition transform reimplemented (`_ilr_np`, `_ilr_coords`, `_ilr_basis`, `_ilr_like_residual`)
- **detail:** The isometric-log-ratio transform for compositional data (race shares) has multiple implementations across reconstruction/measurement. `_ilr_like_residual` (a separate "like" variant) suggests divergent behavior.
- **recommendation:** one `pegasus.measurement.composition` (ILR basis + coords + residual); single source for the race-composition math.

### M5 · `MOD-DUP-TOF64` — LOW — `_to_f64_array` duplicated across `denominators/reconstruction` and `she/reconstruction`
- **detail:** Two parallel reconstruction schema modules (denominators vs she) each define the array-normalization + problem dataclass. The earlier notes flagged `she/reconstruction` vs `denominators/reconstruction` as parallel; this is a duplicated core contract (and M2's float32 change had to be reasoned about in both).
- **recommendation:** determine which reconstruction package is canonical and collapse; a single `PopulationTensorProblem` contract.

### M6 · `MOD-CONTRACT-RELIABILITY` — HIGH — no data contract binding "uncertainty produced" (EFG/§3.12) to "uncertainty consumed" (LDO)
- **detail:** The systemic finding from the math ledger (A1/A13/A20/A22): the EFG layer emits a rich per-field/per-cell reliability state and the LDO ignores it, because there is no typed, enforced interface requiring the estimator to consume it. This is the archetypal missing module boundary — two subsystems that MUST share a contract, communicating through an ignored tensor.
- **recommendation:** a first-class `ObservationReliability` contract (per-cell weight + per-field effective-n + fragility) that `assemble_ldo_tensor` produces and the covariance/SE estimators are REQUIRED to consume, with a contract test asserting a low-reliability cell measurably changes the fit. Both a validity fix (math ledger) and the exemplar of the modularization redesign.

*(Direct survey continues; the `modularization` workflow's full findings — orphan scan, megazord decomposition, API-surface audit — append to Part B.)*

## Part B — `modularization` workflow findings (full, verbatim)
*(pending)*


*(Appended 11 modularization findings verbatim from wf_6c3dfd17-c59; NOTE: 7 of 8 clusters + index FAILED on the session limit and must be re-run — only workflows-seams returned.)*

**`WF-01` - MEDIUM - duplication** - Two identical population-mode→solver-mode mappers in the same compile module
- detail: compile.py defines _compile_population_tensor_mode (line 150) and _population_tensor_mode_to_solver_mode (line 248) — byte-for-byte the same logic (independent_population_tensor→independent_denominator, sim_informed_population_tensor→sim_informed_denominator, else None). _resolve_compile_plan calls the first at line 479; _build_population_tensor_artifact calls the second at line 280. They should collapse into one module-level helper (or one canonical mapping dict on UserIntent/the population domain).
- evidence: src/pegasus/workflows/compile.py:150 (_compile_population_tensor_mode) and :248 (_population_tensor_mode_to_solver_mode); consumed at :479 and :280 respectively.
- recommendation: Delete _population_tensor_mode_to_solver_mode and route line 280 through _compile_population_tensor_mode (rename to a single population_mode_to_solver_mode). Better: move the mapping onto the population domain (pegasus.denominators.population) so both compile and stage_plan share one source of truth.
- risk: The two copies drift: adding a new population_mode string to one but not the other silently disables the tensor solve (returns None) in one code path while the plan says the stage is requested — an unrequested-stage-skipped inconsistency that validate_compiler_stage_plan would only flag by luck.

**`WF-02` - MEDIUM - naming_inconsistency** - Name-collision: two different _race_bridge_prior_artifact functions across pipeline and compile
- detail: pipeline.py:290 _race_bridge_prior_artifact(intent, municipality_cod6) SELECTS/loads a prior from the registry and returns a fresh SourceArtifact; compile.py:237 _race_bridge_prior_artifact(artifacts) FILTERS an existing artifact tuple for the RACE-BRIDGE:emission_prior ref. Same name, opposite semantics (producer vs finder), different signatures — an anti-pattern that makes grep and refactors treacherous across the compile/pipeline seam.
- evidence: src/pegasus/workflows/pipeline.py:290 vs src/pegasus/workflows/compile.py:237 — identical symbol name, incompatible signatures and behaviour.
- recommendation: Rename to disambiguate: pipeline's → _acquire_race_bridge_prior_artifact (it acquires), compile's → _find_race_bridge_prior_ref (it selects from the manifest). The RACE-BRIDGE:emission_prior selection+fixture-rejection logic is duplicated in both (pipeline.py:299-315 vs compile.py:523-563) and should live in registries.race_bridge as one gate.
- risk: A maintainer edits one copy of the fixture/validation-only rejection rule and assumes both paths are covered; the national vs state/smoke paths silently diverge on whether an uncalibrated prior is allowed into a run.

**`WF-03` - HIGH - orphan_deadcode** - run_attach_race_bridge is a ~100-LOC orphan superseded by the autonomous-EFG race path
- detail: report/race_bridge.py:161 run_attach_race_bridge manually builds V_fields/Q_tensor/VariableDictionary rows and attaches a standalone race-bridge field to a run bundle. It has zero callers anywhere (not CLI, not compile, not tests) — grep returns only its definition. The live path is compile.py's _materialize_population_and_substrate, which produces race_bridge fields via the autonomous EFG (build_efg + race_bridge_plan). The manual attacher is exactly the 'manual domain attacher' compile.py:711 says is forbidden ('MSD cutover: manual domain attachers are forbidden').
- evidence: src/pegasus/workflows/report/race_bridge.py:161; grep for run_attach_race_bridge across src+tests returns only the definition. Contrast compile.py:711 comment forbidding manual domain attachers and the autonomous race_bridge_metadata pulled from domain_summaries at compile.py:707.
- recommendation: Delete run_attach_race_bridge plus its private _field_row/_q_row/_vd_row helpers (lines 81-160 are only used by it). Keep run_validate_race_bridge_prior/run_plan_race_bridge if still CLI-wired. The single race-bridge emission path is registries.race_bridge.resolve_race_bridge_plan → autonomous EFG.
- risk: A silent second implementation of race-bridge field construction that hard-codes state='fragile', dashboard_safe=False and its own axes schema; if ever re-wired it would emit fields that diverge from the autonomous EFG's contract, and it rots undetected against Q_tensor/V_fields schema changes.

**`WF-04` - MEDIUM - orphan_deadcode** - build_autonomous_efg / build_autonomous_efg_from_manifest workflow wrappers are bypassed by the real compile path
- detail: construct/build_efg.py:185 build_autonomous_efg and :206 build_autonomous_efg_from_manifest wrap efg.dag.build_efg. But compile.py:16 imports build_efg directly from pegasus.efg.dag and calls it at compile.py:667 — never through the workflow wrapper. build_autonomous_efg is called only by its sibling build_autonomous_efg_from_manifest (:217), which itself has zero non-test callers. So the 'workflow boundary for the autonomous EFG core' is not on any live boundary — the boundary is bypassed.
- evidence: src/pegasus/workflows/construct/build_efg.py:185,206; only internal caller at :217. compile.py:16 `from pegasus.efg.dag import build_efg` and direct call at compile.py:667 — the workflow wrapper is skipped.
- recommendation: Either route compile.py through build_autonomous_efg (making the wrapper the single EFG-construction seam with its default registry_root/operator_budget), or delete both wrappers and the from-manifest variant and let efg.dag.build_efg be the boundary. Do not keep a wrapper nobody enters.
- risk: The wrapper's defaults (registry_root='config/registries', operator_budget=256) are the documented contract but compile passes its own kwargs directly; the two can silently disagree on operator budget/registry root, and readers trust a seam that production never executes.

**`WF-05` - MEDIUM - orphan_deadcode** - build_sim_compiler_run / build_sim_fixture_efg_run fixture-EFG scaffolding lives in the production workflows tree
- detail: construct/build_efg.py:30 build_sim_compiler_run raises 'Manual SIM EFG bundler is retired' (a tombstone with zero callers). build_sim_fixture_efg_run (:138) plus the fixture row-builders _q_row/_vd_row/_field_row/_write_profile_json (:34-135) fabricate a canonical fixture bundle and are used ONLY by tests/unit/test_sidra_denominator_anchor.py. This is ~150 LOC of test-only fixture code sitting in the production workflows package, mixed into the same file as the live build_autonomous_efg.
- evidence: src/pegasus/workflows/construct/build_efg.py:30 (retired tombstone), :138 build_sim_fixture_efg_run used only at tests/unit/test_sidra_denominator_anchor.py:116,135. No production caller.
- recommendation: Delete build_sim_compiler_run outright. Move build_sim_fixture_efg_run + its _q_row/_vd_row/_field_row/_write_profile_json helpers into tests/ (a conftest fixture factory) so production build_efg.py contains only build_autonomous_efg[_from_manifest].
- risk: Fixture builders with hard-coded schema (q_schema_version='1.0', 20+ Q_tensor columns) sit next to the real code and are read as production reference; they silently pin an old bundle schema and give false confidence that the 'canonical' shape is centralized.

**`WF-06` - MEDIUM - duplication** - ingest_sidra duplicates the SIDRA plan→extract→source-manifest flow the pipeline already implements
- detail: acquire/ingest_sidra.py:16 ingest_sidra runs plan_sidra_chunks → extract_chunk_plan → inspect_source_artifact → write_source_artifact_manifest → validate, producing a SIDRA source manifest. The live pipeline implements the same acquire→normalized_facts→inspect flow in acquire/sidra_population.py (_acquire_sidra_population et al.) and combines via sidra_national. ingest_sidra has no production caller (only tests/unit/test_sidra_extract.py) and the refactor plan itself flags it as duplicated (PEGASUS_REFACTOR_MASTER_PLAN.md:277 'whose logic pipeline.py already inlines — redirect or drop').
- evidence: src/pegasus/workflows/acquire/ingest_sidra.py:16; only caller tests/unit/test_sidra_extract.py:126. PEGASUS_REFACTOR_MASTER_PLAN.md:277 acknowledges the duplication.
- recommendation: Drop ingest_sidra and repoint its test at the pipeline's real SIDRA acquisition (_acquire_sidra_population), or make _acquire_sidra_population call ingest_sidra so there is a single SIDRA→source-artifact code path. Do not maintain two.
- risk: Two SIDRA-manifest producers with divergent artifact_role sets (ingest_sidra emits raw_payload + normalized_facts; the pipeline emits normalized_facts/population_strata/civil_registry_*). A schema/role change in one is not reflected in the other, and the test path validates a manifest shape production never emits.

**`WF-07` - MEDIUM - god_module_decomposition** - compile.py is a 1036-LOC god-module: plan/hash/materialize/derive/serialize/flush + population-tensor asset lifecycle all in one file
- detail: compile.py holds the whole compile spine — intent/geo/race-bridge/stage plan resolution (_resolve_compile_plan), source hashing (_hash_and_manifest_sources), population-tensor asset build+slice (_build_population_tensor_artifact, ~137 LOC of asset-store/identity/lifecycle logic), substrate+EFG materialization, pure stage-status derivation, run-bundle serialization (_serialize_run_bundle, ~124 LOC of RunConfig merge), and flush+investigate. The internal phase split into _CompilePlan/_StageStatusResult dataclasses is good, but the population-tensor asset lifecycle and manifest-artifact validation are distinct subsystems glued in-file. Mutation-in-place across phases (_hash_and_manifest_sources mutates plan.source_hashes; _materialize mutates plan.autonomous_artifacts) is an implicit contract, not a typed one.
- evidence: src/pegasus/workflows/compile.py — 1036 lines; _build_population_tensor_artifact:270-406 (asset identity/store/slice), _validate_compile_manifest_artifacts:194-234, _serialize_run_bundle:788-918, all sharing the mutable _CompilePlan.
- recommendation: Extract _build_population_tensor_artifact + _population_tensor_mode_to_solver_mode + _max_period_year into a construct/population_tensor.py; extract _validate_compile_manifest_artifacts + the RACE-BRIDGE prior gate into a source-manifest contract module; leave compile.py as the phase orchestrator. Make the phase functions return new plan fragments instead of mutating _CompilePlan in place.
- risk: In-place mutation across six phase functions means a reordering or an early-return leaves plan.source_hashes / plan.autonomous_artifacts half-populated with no type error; the reproducibility manifest then silently omits a source hash.

**`WF-08` - HIGH - missing_data_contract** - Cross-stage compile↔pipeline↔investigate handoff is untyped dict[str,Any] with getattr/None probing
- detail: The compile→pipeline boundary returns a dict[str,Any] whose 'validation' key is probed with getattr(validation,'ok',False) at pipeline.py:487 and :547 — if run_compile ever changes the key name or returns None, ok silently reads False and the pipeline reports 'compile_failed'/'output bundle validation failed' with no error. compile.py's _run_compile_impl return (compile.py:1001) is a bare dict; run_investigate returns a typed InvestigateResult but _flush_and_investigate immediately re-flattens it into a dict (compile.py:954). There is no typed CompileResult contract shared across the run_live_pipeline → run_compile → run_investigate seam.
- evidence: pipeline.py:486-487 `validation = compile_result.get('validation'); ok = bool(getattr(validation,'ok',False))` (repeated :546-547); compile.py:1001 returns untyped dict; investigate.py:315 returns typed InvestigateResult, re-flattened at compile.py:954-960.
- recommendation: Define a frozen CompileResult dataclass (status, run_dir, validation: OutputBundleValidation, source_hashes, telemetry, race_bridge/…) returned by run_compile, and have pipeline consume result.validation.ok directly. The getattr(...,'ok',False) probe is the exact 'silent-failure via untyped object' the mandate names.
- risk: A renamed/missing 'validation' key makes getattr fall back to False: a genuinely successful national compile is reported as failed (or worse, a failed one as success if the default flipped), with no exception — the reliability of the whole run status hinges on a stringly-typed dict key.

**`WF-09` - MEDIUM - scattered_logic** - Optional-stage 'requested?' predicates are computed twice with different rules (stage_plan vs compile)
- detail: stage_plan.build_compile_stage_plan derives whether population/stdfm/pirs stages are requested from intent (population_requested at stage_plan.py:154, stdfm_requested :159, etc.). But compile.py independently re-derives the SAME decisions: _compile_population_tensor_mode gates the population solver (compile.py:479→_materialize:648), and _derive_stage_status (compile.py:694) recomputes stdfm 'executed/skipped' and the skipped_reasons map from domain_summaries — overwriting the plan's skip_reason_map (compile.py:727-737). Two sources of truth for 'is this stage on', reconciled only loosely by validate_compiler_stage_plan afterward.
- evidence: stage_plan.py:154-172 (population/stdfm/pirs requested predicates) vs compile.py:479 (_compile_population_tensor_mode gates solver) and compile.py:719-737 (_derive_stage_status recomputes stdfm status + skipped_reasons, overriding skip_reason_map).
- recommendation: Make CompilerStagePlan the single authority: compile.py should read plan.by_stage()[id].requested to decide whether to run a stage, and _derive_stage_status should only record success/blocked outcomes against the plan's requested flags — never re-derive 'requested' from intent. Population gating should read the plan, not _compile_population_tensor_mode.
- risk: The plan can say a stage is requested while compile's independent predicate skips it (or vice-versa); validate_compiler_stage_plan then emits requested_stage_skipped/skip_not_allowed, or — because compile overwrites skipped_reasons — masks the inconsistency and the run silently omits a requested inference stage.

**`WF-10` - LOW - orphan_deadcode** - construct/build_substrate.py and efg_materialize.py expose 6 CLI-only wrappers with no pipeline consumer
- detail: build_substrate.py (run_build_substrate_from_artifacts, run_build_substrate_from_source_manifest, run_attach_substrate_to_run, run_substrate_summary) and efg_materialize.py (run_materialize_substrate_manifest, run_attach_efg_materialization_to_run) are thin wrappers over pegasus.she.substrate / pegasus.efg.materialization_manifest. Every one is called ONLY from cli.py manual subcommands (and one test) — never from run_live_pipeline or run_compile, which call build_substrate_bundle directly (compile.py:664). These are the residue of the retired 'granular manual commands' the pipeline docstring (pipeline.py:5) says it replaced; they persist as a parallel manual seam.
- evidence: src/pegasus/workflows/construct/build_substrate.py:16-66 and efg_materialize.py:16-31; callers only at cli.py:509/529/539/574/588 and test_slice14c. Live compile calls build_substrate_bundle directly at compile.py:664, bypassing these wrappers.
- recommendation: If the manual CLI subcommands are still wanted, keep the wrappers but document them as debug-only and co-locate under a cli/ helpers module; otherwise retire the subcommands and delete the wrappers. Do not present them as a construct-stage API the pipeline uses — it does not.
- risk: Two ways to build a substrate (wrapper vs direct build_substrate_bundle) with no shared enforcement; a substrate-contract change tested only through the live path leaves the CLI wrappers emitting a stale manifest schema that users still invoke.

**`WF-11` - LOW - scattered_logic** - _load_intent (json.read → UserIntent.model_validate) is reimplemented in at least four workflow modules
- detail: The intent-file load+validate helper is copy-pasted: pipeline.py:117 _load_intent returns (payload, intent); compile.py:33 _load_intent returns (payload, intent) with a ValidationError→ValueError wrap; report/race_bridge.py:18 _load_intent returns just intent; stage_plan.py has its own _intent_value duck-typing to tolerate dict-or-model. Four slightly different intent-ingestion contracts for one operation, differing in return shape and error handling.
- evidence: src/pegasus/workflows/pipeline.py:117, compile.py:33, report/race_bridge.py:18 (three _load_intent variants) plus stage_plan.py:89 _intent_value.
- recommendation: Add one load_user_intent(path) -> tuple[dict, UserIntent] to pegasus.core.schemas (or a workflows/_intent.py) with the ValidationError→typed-error wrap, and have all four callers import it. stage_plan's dict-or-model tolerance should then be unnecessary because callers always pass a validated UserIntent.
- risk: The compile variant maps ValidationError to a helpful ValueError; the pipeline/race-bridge variants let raw pydantic errors escape. The same malformed intent yields different failure surfaces depending on entrypoint, and a schema-validation tightening must be applied in four places or one path silently accepts an invalid intent.
