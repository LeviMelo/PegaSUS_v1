schema_version: '1.0'
registry_version: v2.0
created_at: '2026-06-06'
updated_at: '2026-06-13'
provenance: Macro-Slice 27A bridge grammar registry for EFG declaration, support,
  and denominator bridge legality.
entries:
- id: race_admin_to_posterior_bridge
  status: active
  bridge_type: race_axis
  from_axis: administrative_race_color
  to_axis: posterior_self_declared_race_color
  operators:
  - Bridge_R
  - Bridge_R_fixedC_dynamicW
  - Bridge_R_posteriorC
  - Bridge_R_localPi_posteriorC
  required_metadata:
  - race_bridge_prior
  - posterior_uncertainty
  - missing_race_observer
  warnings:
  - race_bridge_uncertainty_required
  description: Macro-Slice 27A canonical registry entry race_admin_to_posterior_bridge
    from bridge_grammars.yaml.
- id: geography_geneallocation_bridge
  status: active_artifact_required
  bridge_type: geospatial_support
  operators:
  - geneallocation_measure_pushforward
  - amc_contraction
  required_metadata:
  - crosswalk_manifest_hash
  - mass_conservation_proof
  forbidden_payloads:
  - direct_rate_allocation
  warnings:
  - geospatial_artifact_required
  description: Macro-Slice 27A canonical registry entry geography_geneallocation_bridge
    from bridge_grammars.yaml.
- id: population_denominator_bridge
  status: active
  bridge_type: denominator_support
  operators:
  - RN
  - crude_rate
  - denominator_link
  required_metadata:
  - support_intersection
  - denominator_fragility
  warnings: []
  description: Macro-Slice 27A canonical registry entry population_denominator_bridge
    from bridge_grammars.yaml.
- id: facility_admission_support_bridge
  status: active_warning
  bridge_type: facility_admission_support
  operators:
  - admission_per_bed_ratio
  - admission_capacity_context
  required_metadata:
  - facility_period_support
  - admission_period_support
  warnings:
  - facility_capacity_not_case_denominator
  description: Macro-Slice 27A canonical registry entry facility_admission_support_bridge
    from bridge_grammars.yaml.
- id: morbidity_mortality_divergence_bridge
  status: active
  bridge_type: morbidity_mortality_divergence
  left_carrier: HospitalAdmissions
  right_carrier: Deaths
  operators:
  - divergence_log_ratio
  required_metadata:
  - support_intersection
  warnings:
  - administrative_morbidity_not_incidence
  - divergence_is_observational_not_causal
  description: >-
    Cross-source divergence log(SIH admissions / SIM deaths) on shared (year, municipality)
    support, all-cause and cause-specific (shared ICD stratum). High = treatable/non-fatal
    burden; low = lethal/under-hospitalized burden.
- id: fertility_mortality_divergence_bridge
  status: active
  bridge_type: fertility_mortality_divergence
  left_carrier: LiveBirths
  right_carrier: Deaths
  operators:
  - divergence_log_ratio
  required_metadata:
  - support_intersection
  warnings:
  - divergence_is_observational_not_causal
  description: >-
    Natural-balance divergence log(SINASC live births / SIM deaths) on shared
    (year, municipality) support — a demographic vitality gradient.
- id: morbidity_mortality_divergence_lag1_bridge
  status: active
  bridge_type: morbidity_mortality_lag1_divergence
  left_carrier: HospitalAdmissions
  right_carrier: Deaths
  temporal_lag: 1
  operators:
  - divergence_log_ratio
  required_metadata:
  - support_intersection
  warnings:
  - administrative_morbidity_not_incidence
  - divergence_is_observational_not_causal
  - temporal_lag_divergence_is_observational_not_causal
  description: >-
    Lagged cross-source divergence log(SIH admissions(t-1) / SIM deaths(t)) on shared
    municipality (MSD §2.11) — delayed morbidity-to-mortality gradient. A generic
    demonstration of registry-declared temporal-lag divergences; left operand year is
    shifted by 1 before the shared-support join.
- id: arbovirus_microcephaly_lag1_divergence_bridge
  status: active
  bridge_type: arbovirus_microcephaly_lag1_divergence
  left_carrier: ArbovirusHospitalAdmissions
  right_carrier: MicrocephalyBirths
  temporal_lag: 1
  operators:
  - divergence_log_ratio
  required_metadata:
  - support_intersection
  warnings:
  - administrative_admission_not_incidence
  - divergence_is_observational_not_causal
  - temporal_lag_divergence_is_observational_not_causal
  - sparse_restricted_denominator_fragile
  description: >-
    Lagged restricted-carrier divergence log(SIH arbovirus admissions(t-1) /
    SINASC microcephaly births(t)) on shared municipality (MSD §2.11) — the
    Zika-epidemic coupling gradient. Restricted-carrier divergence; microcephaly is
    a sparse denominator so the field is expected fragile/quarantined where counts
    are too low — surfaced honestly, never as a verified claim.
