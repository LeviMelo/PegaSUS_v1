# Race Bridge

Race-axis transformations are registry-backed and attach explicit prior, uncertainty, and sensitivity evidence. Unsupported or missing priors block the branch; they are never inferred silently.

Compile telemetry records whether the bridge executed or was skipped by intent. Resulting fields preserve source and target race-axis provenance.
