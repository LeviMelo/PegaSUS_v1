# Registries

Registries define source fields, operators, null regimes, population solvers, race bridges, geospatial sources, and quality permissions. Production decisions must resolve through canonical registry loaders and record registry hashes in the reproducibility manifest.

Legacy or scaffold entries may remain only as deprecated, non-selected reproducibility metadata. Required production entries must be active and validated.
