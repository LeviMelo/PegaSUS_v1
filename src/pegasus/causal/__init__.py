"""Causal escalation ladder (MSD-III Part IV).

The LDO yields an associational + temporal-precedence skeleton; the causal ladder is a
typed set of rungs on top, each adding assumptions and labelled by them. Rung-1
(``orient``) auto-applies only where the assumptions are machine-checkable: non-Gaussian
orientation (LiNGAM / additive-noise) and collider/v-structure detection.
"""
